::: center
Министерство образования и науки Российской Федерации\
Санкт-Петербургский политехнический университет Петра Великого\
Институт компьютерных наук и кибербезопасности\
Высшая школа технологий искусственного интелекта\
Направление: 02.03.01 «Математика и компьютерные науки»\
Отчёт о выполниии работы по дисцилине\
Научно-исследовательская работа (получение первичных навыков
научно-исследовательской работы)\
:::

+:---------------------+--------------:+:--------------:+
| Выполнил студент     |               |                |
+----------------------+---------------+----------------+
| группы 5130201/40002 | (1,0)100      | Семенов И. А.  |
+----------------------+---------------+----------------+
|                      |               |                |
+----------------------+---------------+----------------+
| Проверил             |               |                |
+----------------------+---------------+----------------+
| преподователь        | (1,0)100      | Глазунов В. В. |
+----------------------+---------------+----------------+
|                      |               |                |
+----------------------+---------------+----------------+
|                      |               |                |
+----------------------+---------------+----------------+
|                      |               |                |
+----------------------+---------------+----------------+
|                      | (1,0)30 (1,0)100   2025г.      |
+----------------------+--------------------------------+

\

Санкт-Петербург

# Введение {#введение .unnumbered}

Проводилось изучение и применение технологий виртуализации,
взаимодействия с операционной системой семейства GNU/Linux, а также
разработка и тестирование программного обеспечения на языке
программирования Си. Основной целью было закрепление навыков работы в
текстовом терминале без графической оболочки, настройка изолированной
среды разработки, изучение механизма линковки и работы с библиотеками,
включая исследование зависимости исполняемых файлов от разделяемых
библиотек.

В качестве среды виртуализации использовалась VirtualBox, установленная
на машине с архитектурой ARM64 (Apple Silicon, процессор Apple M3), что
потребовало выбора соответствующего дистрибутива Debian 12 под
архитектуру `arm64`. Гостевая система устанавливалась в режиме без
графической оболочки, что обеспечило минимальный размер системы.

В процессе выполнения заданий были задействованы следующие технологии:
удалённый доступ по протоколу SSH, асимметричная криптография для
настройки беспарольной аутентификации, работа с компилятором `gcc`,
построение статических и динамических библиотек, сборка проектов с
помощью `cmake`, анализ зависимостей с помощью `ldd`, а также создание
интерфейсов в псевдографике с использованием библиотеки `ncurses`.

В последующих разделах отчёта будут представлены результаты настройки
виртуальной среды, реализации и тестирования программ, а также анализа
их производительности. Будут рассмотрены особенности работы со
статическими и динамическими библиотеками, влияние различных флагов
оптимизации компилятора на скорость выполнения программ, а также процесс
разработки и отладки программ, взаимодействующих с бинарными файлами и
пользовательским вводом в текстовом интерфейсе.

# Постановка задачи

В рамках данной работы требуется реализовать и протестировать комплекс
программ, демонстрирующих работу со статическими и динамическими
библиотеками, а также разработать приложение для управления бинарной
базой данных с визуализацией в псевдографике на основе библиотеки
`ncurses` в среде GNU/Linux.

### Исходные данные. {#исходные-данные. .unnumbered}

- В качестве среды разработки используется виртуальная машина VirtualBox
  с установленным дистрибутивом Debian 12 под архитектуру `arm64` (Apple
  Silicon, Apple M3), настроенная для работы без графической оболочки
  (ранее уже оговаривалось во введении).

- Используется компилятор `gcc`, системы сборки `cmake` и инструменты
  анализа зависимостей и бинарных данных (`ldd`, `hexdump`).

- Для интерфейса программы использован пакет `ncurses`, позволяющий
  создавать текстовые интерфейсы в терминале.

- Контроль целостности данных реализуется с использованием CRC-32 из
  библиотеки `zlib`.

### При выполнении работы необходимо: {#при-выполнении-работы-необходимо .unnumbered}

1.  Разработать библиотеку нахождения максимального элемента в массиве,
    реализованную двумя способами --- с рекурсией и без, без
    использования библиотечных функций. Оформить её в виде статической и
    динамической библиотек.

2.  Подготовить тестовую программу, сравнивающую производительность
    работы библиотеки при различных вариантах линковки (статической и
    динамической), а также при подгрузке библиотеки во время выполнения
    через `dlopen`. Провести замеры времени исполнения с помощью `time`
    для разных уровней оптимизации компилятора (-O0, -O1, -O2, -Os), и
    сделать выводы о влиянии оптимизаций.

3.  Исследовать зависимости скомпилированных программ от разделяемых
    библиотек с помощью команды `ldd` и визуализировать полученные
    зависимости в виде рекурсивного дерева.

4.  Описать структуру `ZNAK`, содержащую следующие поля:

    - фамилия и инициалы (строка языка Си),

    - знак зодиака (строка языка Си),

    - год рождения (целое число).

5.  Разработать бинарный формат хранения базы данных, включающий
    заголовок с полями:

    - сигнатура (4 байта) --- первые четыре буквы фамилии латиницей,

    - номер транзакции (4 байта), увеличивающийся при каждом чтении и
      записи,

    - количество структур `ZNAK` (4 байта),

    - контрольная сумма CRC-32 (4 байта), вычисляемая для данных,
      следующих за заголовком.

6.  Реализовать программу с использованием библиотеки `ncurses`,
    выполняющую:

    - создание нового бинарного файла базы данных при отсутствии файла,
      с вводом массива структур `ZNAK` через псевдографический
      интерфейс,

    - поиск и вывод информации о людях, родившихся в указанный год с
      заданным знаком зодиака (или сообщение об отсутствии таких
      записей),

    - добавление новой записи и обновление заголовка с корректировкой
      CRC-32.

7.  Зафиксировать промежуточные состояния бинарного файла (например, с
    помощью `hexdump`), продемонстрировав изменения заголовка после
    операций с файлом.

8.  Провести анализ зависимости итоговой программы от динамических
    библиотек через `ldd` и построить соответствующее рекурсивное
    дерево.

### Ожидаемые результаты. {#ожидаемые-результаты. .unnumbered}

В результате выполнения работы должны быть получены:

- Статическая и динамическая библиотеки с функцией поиска максимума, а
  также отчёт о сравнении их производительности.

- Исполняемая программа для управления базой данных в формате бинарного
  файла с заголовком и массивом структур `ZNAK`.

- Подробный отчёт с демонстрацией работы программы в текстовом
  интерфейсе (скриншоты), выводами утилит `ldd`, `hexdump`, и схемой
  рекурсивного дерева библиотечных зависимостей.

# Особенности реализации

## Установка и настройка среды виртуализации

### Установка среды VirtualBox

В качестве основной платформы виртуализации использовалась среда
`VirtualBox` --- свободно распространяемый гипервизор, обеспечивающий
изоляцию и запуск гостевых операционных систем. Установка VirtualBox
была выполнена на хостовой системе с операционной системой macOS под
управлением процессора Apple M3 (архитектура ARM64). Данная среда
позволяет создавать виртуальные машины с заданными параметрами,
управлять виртуальными сетями и использовать гибкую систему образов
дисков. Установка производилась с официального сайта проекта VirtualBox.

### Установка и загрузка гостевой операционной системы GNU/Linux Debian 12

Для установки гостевой операционной системы использовался официальный
минимальный образ `debian-12.11.0-arm64-netinst.iso`, скачанный с сайта
проекта Debian. Архитектура ARM64 была выбрана в соответствии с
платформой Apple M3, на которой запускалась виртуальная машина. На
скриншоте
[1](#fig:Screenshot_2025-06-18_at_19.10.15.png){reference-type="ref"
reference="fig:Screenshot_2025-06-18_at_19.10.15.png"} приведён сайт
загрузки установочного образа.

![Страница загрузки установочного образа Debian 12 для
ARM64.](img/Screenshot_2025-06-18_at_19.10.15.png){#fig:Screenshot_2025-06-18_at_19.10.15.png
width="90%"}

Установка операционной системы производилась в текстовом режиме.

Процесс установки включал следующие шаги.

- В меню загрузчика выбрали пункт `Install`, чтобы начать установку в
  текстовом режиме
  (рис. [2](#fig:VirtualBox_summer_p_semenov__01_07_2025_15_12_41.png){reference-type="ref"
  reference="fig:VirtualBox_summer_p_semenov__01_07_2025_15_12_41.png"}).

- На этапе выбора языка интерфейса установки указали `English`
  (рис. [3](#fig:VirtualBox_summer_p_semenov__01_07_2025_15_13_13.png){reference-type="ref"
  reference="fig:VirtualBox_summer_p_semenov__01_07_2025_15_13_13.png"}).

- Для региона (локали) выбрали `United States`
  (рис. [4](#fig:VirtualBox_summer_p_semenov__01_07_2025_15_13_28.png){reference-type="ref"
  reference="fig:VirtualBox_summer_p_semenov__01_07_2025_15_13_28.png"}).

- Для раскладки клавиатуры оставили `American English`
  (рис. [5](#fig:VirtualBox_summer_p_semenov__01_07_2025_15_13_37.png){reference-type="ref"
  reference="fig:VirtualBox_summer_p_semenov__01_07_2025_15_13_37.png"}).

- Дождались автоматической настройки сети по DHCP
  (рис. [6](#fig:VirtualBox_summer_p_semenov__01_07_2025_15_13_52.png){reference-type="ref"
  reference="fig:VirtualBox_summer_p_semenov__01_07_2025_15_13_52.png"}).

- Задали имя хоста (`vbox`) для виртуальной машины
  (рис. [7](#fig:VirtualBox_summer_p_semenov__01_07_2025_15_14_01.png){reference-type="ref"
  reference="fig:VirtualBox_summer_p_semenov__01_07_2025_15_14_01.png"}).

- Поле доменного имени оставили пустым
  (рис. [8](#fig:VirtualBox_summer_p_semenov__01_07_2025_15_14_13.png){reference-type="ref"
  reference="fig:VirtualBox_summer_p_semenov__01_07_2025_15_14_13.png"}).

- Установили и подтвердили пароль пользователя `root`
  (рис. [9](#fig:VirtualBox_summer_p_semenov__01_07_2025_15_14_28.png){reference-type="ref"
  reference="fig:VirtualBox_summer_p_semenov__01_07_2025_15_14_28.png"},
  [10](#fig:VirtualBox_summer_p_semenov__01_07_2025_15_14_37.png){reference-type="ref"
  reference="fig:VirtualBox_summer_p_semenov__01_07_2025_15_14_37.png"}).

- Указали полное имя пользователя (`Ilya Semenov`)
  (рис. [11](#fig:VirtualBox_summer_p_semenov__01_07_2025_15_15_37.png){reference-type="ref"
  reference="fig:VirtualBox_summer_p_semenov__01_07_2025_15_15_37.png"}).

- Указали имя пользователя для входа в систему (`semenov`)
  (рис. [12](#fig:VirtualBox_summer_p_semenov__01_07_2025_15_15_42.png){reference-type="ref"
  reference="fig:VirtualBox_summer_p_semenov__01_07_2025_15_15_42.png"}).

- Задали пароль для нового пользователя и подтвердили его
  (рис. [13](#fig:VirtualBox_summer_p_semenov__01_07_2025_15_15_52.png){reference-type="ref"
  reference="fig:VirtualBox_summer_p_semenov__01_07_2025_15_15_52.png"},
  [14](#fig:VirtualBox_summer_p_semenov__01_07_2025_15_15_57.png){reference-type="ref"
  reference="fig:VirtualBox_summer_p_semenov__01_07_2025_15_15_57.png"}).

- Выбрали диск для разметки (виртуальный диск `sda` на 21.5 ГБ)
  (рис. [15](#fig:VirtualBox_summer_p_semenov__01_07_2025_15_16_24.png){reference-type="ref"
  reference="fig:VirtualBox_summer_p_semenov__01_07_2025_15_16_24.png"}).

- Выбрали схему разметки
  `All files in one partition (recommended for new users)`
  (рис. [16](#fig:VirtualBox_summer_p_semenov__01_07_2025_15_16_30.png){reference-type="ref"
  reference="fig:VirtualBox_summer_p_semenov__01_07_2025_15_16_30.png"}).

- Подтвердили разметку и запись изменений на диск
  (рис. [17](#fig:VirtualBox_summer_p_semenov__01_07_2025_15_16_36.png){reference-type="ref"
  reference="fig:VirtualBox_summer_p_semenov__01_07_2025_15_16_36.png"}).

- На этапе настройки зеркала выбрали `deb.debian.org`
  (рис. [18](#fig:VirtualBox_summer_p_semenov__01_07_2025_15_17_52.png){reference-type="ref"
  reference="fig:VirtualBox_summer_p_semenov__01_07_2025_15_17_52.png"}).

- После завершения установки получили сообщение о завершении и готовы к
  перезагрузке
  (рис. [19](#fig:VirtualBox_summer_p_semenov__01_07_2025_15_20_06.png){reference-type="ref"
  reference="fig:VirtualBox_summer_p_semenov__01_07_2025_15_20_06.png"}).

Данные шаги отображены на скриншотах ниже.

![Старт установки Debian, выбор текстового
режима.](img/VirtualBox_summer_p_semenov__01_07_2025_15_12_41.png){#fig:VirtualBox_summer_p_semenov__01_07_2025_15_12_41.png
width="90%"}

![Выбор языка
установки.](img/VirtualBox_summer_p_semenov__01_07_2025_15_13_13.png){#fig:VirtualBox_summer_p_semenov__01_07_2025_15_13_13.png
width="90%"}

![Выбор страны
(локали).](img/VirtualBox_summer_p_semenov__01_07_2025_15_13_28.png){#fig:VirtualBox_summer_p_semenov__01_07_2025_15_13_28.png
width="90%"}

![Выбор раскладки
клавиатуры.](img/VirtualBox_summer_p_semenov__01_07_2025_15_13_37.png){#fig:VirtualBox_summer_p_semenov__01_07_2025_15_13_37.png
width="90%"}

![Настройка сети через
DHCP.](img/VirtualBox_summer_p_semenov__01_07_2025_15_13_52.png){#fig:VirtualBox_summer_p_semenov__01_07_2025_15_13_52.png
width="90%"}

![Указание имени
хоста.](img/VirtualBox_summer_p_semenov__01_07_2025_15_14_01.png){#fig:VirtualBox_summer_p_semenov__01_07_2025_15_14_01.png
width="90%"}

![Пропуск ввода доменного
имени.](img/VirtualBox_summer_p_semenov__01_07_2025_15_14_13.png){#fig:VirtualBox_summer_p_semenov__01_07_2025_15_14_13.png
width="90%"}

![Установка пароля
root.](img/VirtualBox_summer_p_semenov__01_07_2025_15_14_28.png){#fig:VirtualBox_summer_p_semenov__01_07_2025_15_14_28.png
width="90%"}

![Подтверждение пароля
root.](img/VirtualBox_summer_p_semenov__01_07_2025_15_14_37.png){#fig:VirtualBox_summer_p_semenov__01_07_2025_15_14_37.png
width="90%"}

![Ввод полного имени
пользователя.](img/VirtualBox_summer_p_semenov__01_07_2025_15_15_37.png){#fig:VirtualBox_summer_p_semenov__01_07_2025_15_15_37.png
width="90%"}

![Ввод имени пользователя для входа в
систему.](img/VirtualBox_summer_p_semenov__01_07_2025_15_15_42.png){#fig:VirtualBox_summer_p_semenov__01_07_2025_15_15_42.png
width="90%"}

![Установка пароля
пользователя.](img/VirtualBox_summer_p_semenov__01_07_2025_15_15_52.png){#fig:VirtualBox_summer_p_semenov__01_07_2025_15_15_52.png
width="90%"}

![Подтверждение пароля
пользователя.](img/VirtualBox_summer_p_semenov__01_07_2025_15_15_57.png){#fig:VirtualBox_summer_p_semenov__01_07_2025_15_15_57.png
width="90%"}

![Выбор диска для
установки.](img/VirtualBox_summer_p_semenov__01_07_2025_15_16_24.png){#fig:VirtualBox_summer_p_semenov__01_07_2025_15_16_24.png
width="90%"}

![Выбор схемы
разметки.](img/VirtualBox_summer_p_semenov__01_07_2025_15_16_30.png){#fig:VirtualBox_summer_p_semenov__01_07_2025_15_16_30.png
width="90%"}

![Подтверждение записи разметки на
диск.](img/VirtualBox_summer_p_semenov__01_07_2025_15_16_36.png){#fig:VirtualBox_summer_p_semenov__01_07_2025_15_16_36.png
width="90%"}

![Выбор зеркала репозитория
Debian.](img/VirtualBox_summer_p_semenov__01_07_2025_15_17_52.png){#fig:VirtualBox_summer_p_semenov__01_07_2025_15_17_52.png
width="90%"}

![Завершение установки и готовность к
перезагрузке.](img/VirtualBox_summer_p_semenov__01_07_2025_15_20_06.png){#fig:VirtualBox_summer_p_semenov__01_07_2025_15_20_06.png
width="90%"}

### Настройка  портов для подключения к гостевой ОС по SSH

Для обеспечения доступа к гостевой системе по протоколу SSH необходимо
настроить  порта 22 из виртуальной машины на свободный порт хостовой
операционной системы. Это позволяет подключаться к гостевой Debian из
macOS с использованием локального IP-адреса и перенаправленного порта,
что особенно удобно при работе через терминал.

В настройках сети виртуальной машины в VirtualBox был использован
сетевой адаптер типа `NAT`. Далее во вкладке `Port Forwarding` было
добавлено новое правило: хостовый порт 2222 (на `127.0.0.1`)
перенаправляется на порт 22 гостевой системы.

Настройка правила  порта приведена на скриншоте
[20](#fig:Screenshot_2025-06-18_at_19.56.16.png){reference-type="ref"
reference="fig:Screenshot_2025-06-18_at_19.56.16.png"}.

![Настройка  22 порта SSH на 2222 порт хоста в
VirtualBox.](img/Screenshot_2025-06-18_at_19.56.16.png){#fig:Screenshot_2025-06-18_at_19.56.16.png
width="90%"}

### Приглашение к вводу имени пользователя после загрузки системы {#приглашение-к-вводу-имени-пользователя-после-загрузки-системы .unnumbered}

После завершения установки и перезагрузки гостевой операционной системы
Debian 12 система автоматически запускается в текстовом терминале
`tty1`, без графической оболочки. На экране появляется приглашение к
вводу имени пользователя, оформленное в виде строки (рис.
[21](#fig:VirtualBox_summer_p_semenov_25_06_2025_22_24_04.png){reference-type="ref"
reference="fig:VirtualBox_summer_p_semenov_25_06_2025_22_24_04.png"}).

![Экран приглашения к вводу логина после загрузки в терминале
`tty1`.](img/VirtualBox_summer_p_semenov_25_06_2025_22_24_04.png){#fig:VirtualBox_summer_p_semenov_25_06_2025_22_24_04.png
width="90%"}

Это подтверждает успешную загрузку системы, правильную настройку учётной
записи и работу без графического интерфейса.

Ниже представлен успешный вход в систему под созданным ранее
пользователем `semenov` (рис.
[22](#fig:VirtualBox_summer_p_semenov_18_06_2025_19_57_43.png){reference-type="ref"
reference="fig:VirtualBox_summer_p_semenov_18_06_2025_19_57_43.png"}).

![Успешный вход в систему: отображение имени пользователя, имени хоста и
приглашения командной
строки.](img/VirtualBox_summer_p_semenov_18_06_2025_19_57_43.png){#fig:VirtualBox_summer_p_semenov_18_06_2025_19_57_43.png
width="90%"}

## Основы работы в командном интерпретаторе

### Подключение к командному интерпретатору через SSH и проверка службы

Для удалённого доступа к гостевой операционной системе Debian 12 с
хостовой машины (macOS) использовался встроенный SSH-клиент. Подключение
производилось к адресу `127.0.0.1` и проброшенному порту `2222`,
настроенному ранее в VirtualBox. Команда подключения выглядела следующим
образом:

    ssh semenov@127.0.0.1 -p 2222

При первом подключении была выведена информация о ключе хоста и
предупреждение об отсутствии записи в `known_hosts`. После подтверждения
и успешного ввода пароля пользователь получил доступ к командному
интерпретатору гостевой системы. На скриншоте
[23](#fig:Screenshot_2025-06-18_at_20.04.45.png){reference-type="ref"
reference="fig:Screenshot_2025-06-18_at_20.04.45.png"} зафиксирован
момент установления соединения.

![Успешное подключение к командному интерпретатору через SSH с хостовой
системы.](img/Screenshot_2025-06-18_at_20.04.45.png){#fig:Screenshot_2025-06-18_at_20.04.45.png
width="90%"}

Для проверки состояния SSH-сервера внутри гостевой ОС была выполнена
команда:

    systemctl status ssh

Результат подтвердил, что служба `ssh.service` активна и запущена. Это
позволяет использовать как вход с паролем, так и последующую настройку
беспарольной аутентификации. Также на скриншоте
[24](#fig:VirtualBox_summer_p_semenov_18_06_2025_20_02_29.png){reference-type="ref"
reference="fig:VirtualBox_summer_p_semenov_18_06_2025_20_02_29.png"}
видно отсутствие утилиты `sudo`, что объясняется минимальной установкой
системы без дополнительных пакетов.

![Проверка статуса службы OpenSSH на стороне гостевой ОС Debian
12.](img/VirtualBox_summer_p_semenov_18_06_2025_20_02_29.png){#fig:VirtualBox_summer_p_semenov_18_06_2025_20_02_29.png
width="90%"}

### Редактирование команд в командной строке

Одной из ключевых особенностей командной оболочки Linux является
возможность быстрого и удобного редактирования вводимых команд. Это
позволяет эффективно исправлять ошибки, перемещаться по строке и
редактировать команды без необходимости повторного ввода.

- **←**, **→** --- перемещение курсора влево и вправо по символам.

- **BackSpace** --- удаление символа слева от курсора.

- **Del** --- удаление символа под курсором (справа).

- **\^h** --- эквивалентно `BackSpace`; используется как клавиатурное
  сочетание `Ctrl + H`.

- **\^w** --- удаляет слово слева от курсора.

- **\^u** --- удаляет всю строку от начала до текущего положения
  курсора.

Данные клавиши особенно полезны при работе в терминале без графического
интерфейса, когда перепечатывание длинных команд может привести к
ошибкам и потере времени. Также это хорошая альтернатива мыши при
удалённой работе через SSH.

**Пример:**

Ввод команды с ошибкой: `touch /hoem/user/test.txt` В имени директории
опечатка: `/hoem` вместо `/home`. Курсор перемещается с помощью клавиш
`←` до начала слова `hoem`, затем нажимается `Ctrl + w` для удаления
неверного слова. Вводится корректный путь: `home`, в итоге команда
становится `touch /home/user/test.txt`.

### Автодополнение и прерывание команд {#автодополнение-и-прерывание-команд .unnumbered}

Одной из удобных возможностей командного интерпретатора является
автодополнение команд с помощью клавиши `Tab`. Эта функция позволяет не
только ускорить ввод, но и избежать опечаток в командах. В рамках
практики было протестировано автодополнение на примере нескольких
базовых утилит.

При вводе первых символов команды, например `up`, нажатие клавиши `Tab`
выводит доступные совпадения: `update-alternatives`, `uptime` и др.
Далее пользователь может либо продолжить ввод вручную, либо нажать `Tab`
повторно для автозавершения, если совпадение однозначно.

На скриншоте
[25](#fig:Screenshot_2025-06-26_at_09.47.08.png){reference-type="ref"
reference="fig:Screenshot_2025-06-26_at_09.47.08.png"} приведён пример
автодополнения команды `uptime` и вызова команды `time` без аргументов,
что выводит отчёт с нулевыми значениями времени.

![Автодополнение команд и использование утилиты
`time`.](img/Screenshot_2025-06-26_at_09.47.08.png){#fig:Screenshot_2025-06-26_at_09.47.08.png
width="90%"}

Также в процессе практики были протестированы механизмы прерывания
команд:

- `Ctrl + C` --- немедленное прерывание текущей команды или процесса.

- `q` --- завершение вывода в утилитах постраничного просмотра (`less`,
  `man`, `hexdump`).

- `Ctrl + D` --- отправка признака конца ввода (EOF), используется для
  выхода из ввода или завершения сессии.

### Использование справочной системы `man` {#использование-справочной-системы-man .unnumbered}

В операционной системе GNU/Linux для получения справки по командам и
системным вызовам используется утилита `man`. Справочные страницы (*man
pages*) содержат описание синтаксиса, аргументов, ключей и примеры
использования.

При вызове справки по команде `cp` с помощью:

    man cp

выведено предупреждение (рис.
[27](#fig:Screenshot_2025-06-26_at_09.59.25.png){reference-type="ref"
reference="fig:Screenshot_2025-06-26_at_09.59.25.png"}):
`man: can’t set the locale; make sure $LC_* and $LANG are correct`. Оно
связано с тем, что в минимальной установке Debian отсутствуют настройки
локали. Тем не менее, справочная страница отобразилась корректно (рис.
[26](#fig:Screenshot_2025-06-26_at_09.55.49.png){reference-type="ref"
reference="fig:Screenshot_2025-06-26_at_09.55.49.png"}).

![Справка по команде
`cp`.](img/Screenshot_2025-06-26_at_09.55.49.png){#fig:Screenshot_2025-06-26_at_09.55.49.png
width="90%"}

`man`-страница включает описание команды `cp` и возможных ключей
(например, `-r`, `-a`, `--backup`, и др.).

![Ошибка локали при запуске
`man cp`.](img/Screenshot_2025-06-26_at_09.59.25.png){#fig:Screenshot_2025-06-26_at_09.59.25.png
width="90%"}

Полученные сведения были сразу же применены при выполнении команды
копирования. В ходе практики был создан файл `test.txt`, который затем
копировался в подкаталог `politech`. Команда:

    cp ./* politech

только для файлов, но не для подкаталогов, что привело к сообщению:

    cp: -r not specified; omitting directory './politech'

выполнения команды `ls politech` можно убедиться, что файл `test.txt`
был успешно скопирован, а каталоги были проигнорированы из-за отсутствия
ключа `-r` (рис.
[28](#fig:Screenshot_2025-06-26_at_11.47.45.png){reference-type="ref"
reference="fig:Screenshot_2025-06-26_at_11.47.45.png"}).

![Практическое применение информации из
`man cp`.](img/Screenshot_2025-06-26_at_11.47.45.png){#fig:Screenshot_2025-06-26_at_11.47.45.png
width="90%"}

### Назначение команд и целевые действия

В ходе практики были опробованы и изучены базовые команды GNU/Linux,
перечисленные разделе **работы в командном интерпретаторе**. Для каждой
команды была открыта справочная страница с помощью `man` и выполнены
практические действия. Ниже приведён краткий обзор:

- `whoami` --- выводит имя текущего пользователя.

- `date` --- отображает текущие дату и время.

- `cal` --- показывает календарь на текущий или заданный месяц/год.

- `uptime` --- показывает время работы системы, количество пользователей
  и среднюю загрузку.

- `df` --- показывает информацию об использовании дискового пространства
  (по умолчанию в 1К-блоках).

- `cp` --- копирует файлы и каталоги. Пример описан в разделе выше.

- `mv` --- перемещает (или переименовывает) файлы и каталоги.

- `touch` --- создаёт новый пустой файл или обновляет временные метки
  существующего.

- `rm` --- удаляет файлы и каталоги. Требует осторожности, особенно с
  опцией `-r`.

- `stat` --- отображает подробную информацию о файле: размер, время
  доступа и изменения, права.

- `file` --- определяет тип содержимого файла.

- `find` --- ищет файлы и каталоги по имени и другим критериям.

- `whereis` --- показывает расположение бинарного файла, исходников и
  справки по имени команды.

- `cat` --- выводит содержимое файла в стандартный вывод.

- `hexdump` --- отображает бинарное содержимое файла в шестнадцатеричном
  виде.

- `less` --- постраничный просмотр длинных текстов, например:

- `grep` --- поиск по шаблону (регулярному выражению) в текстовых
  данных.

- `time` --- измеряет продолжительность выполнения команды. Без
  аргументов просто выводит нули.

### Примеры выполнения команд и их результатов

На скриншоте
[29](#fig:Screenshot_2025-06-26_at_12.28.01){reference-type="ref"
reference="fig:Screenshot_2025-06-26_at_12.28.01"} показан вывод ряда
базовых команд: `whoami`, `date`, `uptime` и `df`. Команда `whoami`
отобразила имя текущего пользователя, `date` вывела текущие дату и
время, `uptime` сообщила о времени непрерывной работы системы и нагрузке
на процессор, а `df` показала информацию об использовании дискового
пространства. Попытка вызова команды `cal` завершилась ошибкой, так как
утилита не была установлена в системе.

![Вывод базовых
команд.](img/Screenshot_2025-06-26_at_12.28.01){#fig:Screenshot_2025-06-26_at_12.28.01
width="90%"}

Затем был создан каталог `files` с помощью команды `mkdir`, а также
пустой файл `file.txt` командой `touch`. Это подготовило рабочую
структуру для выполнения последующих операций с файлами (рис.
[30](#fig:Screenshot_2025-06-26_at_12.28.52){reference-type="ref"
reference="fig:Screenshot_2025-06-26_at_12.28.52"}).

![Создание каталога и
файла.](img/Screenshot_2025-06-26_at_12.28.52){#fig:Screenshot_2025-06-26_at_12.28.52
width="90%"}

Содержимое файла `file.txt` было отредактировано в терминальном
текстовом редакторе `nano`. В файл была добавлена строка `Hello!`, после
чего он был сохранён и закрыт (рис.
[31](#fig:Screenshot_2025-06-26_at_12.29.30){reference-type="ref"
reference="fig:Screenshot_2025-06-26_at_12.29.30"}).

![Редактирование файла в
`nano`.](img/Screenshot_2025-06-26_at_12.29.30){#fig:Screenshot_2025-06-26_at_12.29.30
width="90%"}

На следующем этапе показано использование команд `mv`, `cp` и `rm`. Файл
был перемещён в подкаталог, затем скопирован обратно в текущую
директорию, а после этого удалён. Также была применена команда `stat`,
позволившая получить расширенную информацию о файле: его размер, права
доступа, дату создания и последнего изменения (рис.
[32](#fig:Screenshot_2025-06-26_at_12.31.35){reference-type="ref"
reference="fig:Screenshot_2025-06-26_at_12.31.35"}).

![Работа с файлами и
метаданными.](img/Screenshot_2025-06-26_at_12.31.35){#fig:Screenshot_2025-06-26_at_12.31.35
width="90%"}

Команда `file` определила тип содержимого файла как текстовый. Утилита
`find` успешно нашла все `.txt`-файлы в текущем каталоге. С помощью
`whereis` был определён путь к исполняемому файлу `cp` и его
документации. Затем выполнены команды `cat` и `hexdump`,
продемонстрировавшие содержимое файла в обычном и шестнадцатеричном
представлении соответственно (рис.
[33](#fig:Screenshot_2025-06-26_at_12.34.36){reference-type="ref"
reference="fig:Screenshot_2025-06-26_at_12.34.36"}).

![Анализ и просмотр
файлов.](img/Screenshot_2025-06-26_at_12.34.36){#fig:Screenshot_2025-06-26_at_12.34.36
width="90%"}

Далее файл был открыт с помощью `less` --- удобного инструмента для
просмотра содержимого с возможностью прокрутки. Управление просмотром
осуществляется клавишами, а выход из режима просмотра выполняется
нажатием `q` (рис.
[34](#fig:Screenshot_2025-06-26_at_12.34.47){reference-type="ref"
reference="fig:Screenshot_2025-06-26_at_12.34.47"}).

![Просмотр текста через
`less`.](img/Screenshot_2025-06-26_at_12.34.47){#fig:Screenshot_2025-06-26_at_12.34.47
width="90%"}

Наконец, команда `time` была вызвана без аргументов, что привело к
мгновенному завершению с нулевыми значениями времени исполнения. Также
показано удаление каталога `files` с помощью `rm -rf` (рис.
[35](#fig:Screenshot_2025-06-26_at_12.35.35){reference-type="ref"
reference="fig:Screenshot_2025-06-26_at_12.35.35"}).

![Удаление каталога и использование
`time`.](img/Screenshot_2025-06-26_at_12.35.35){#fig:Screenshot_2025-06-26_at_12.35.35
width="90%"}

### Работа с историей команд

В командной строке GNU/Linux доступна удобная навигация по ранее
введённым командам с помощью клавиш со стрелками: **↑** --- для перехода
к предыдущим командам, **↓** --- к последующим. Это позволяет быстро
повторять, корректировать или комбинировать уже выполненные команды без
необходимости повторного ручного ввода.

Для вывода полной истории всех команд используется команда `history`. На
скриншоте
[36](#fig:Screenshot_2025-06-26_at_14.18.22){reference-type="ref"
reference="fig:Screenshot_2025-06-26_at_14.18.22"} показан результат её
выполнения.

Последними тремя командами в истории стали:

1.  `man cal` --- попытка обратиться к справке по команде `cal`;

2.  `ls` --- вывод содержимого текущего каталога;

3.  `rm -rf files` --- удаление каталога `files` со всем его содержимым.

![История команд, полученная с помощью
`history`.](img/Screenshot_2025-06-26_at_14.18.22){#fig:Screenshot_2025-06-26_at_14.18.22
width="90%"}

### Настройка аутентификации по ключу {#настройка-аутентификации-по-ключу .unnumbered}

Важной частью работы с терминалом и удалёнными серверами является
настройка безопасного и удобного способа входа --- с помощью пары
приватного и публичного ключей. Это исключает необходимость каждый раз
вводить пароль, а также повышает безопасность подключения.

На первом этапе была сгенерирована новая пара ключей с помощью команды:

    ssh-keygen -t ed25519 -C "practice_key"

При этом была создана пара файлов `id_ed25519` (приватный ключ) и
`id_ed25519.pub` (публичный ключ) в домашнем каталоге пользователя
macOS. Это показано на скриншоте
[37](#fig:Screenshot_2025-06-19_at_17.18.44.png){reference-type="ref"
reference="fig:Screenshot_2025-06-19_at_17.18.44.png"}.

![Генерация
SSH-ключа.](img/Screenshot_2025-06-19_at_17.18.44.png){#fig:Screenshot_2025-06-19_at_17.18.44.png
width="90%"}

.

Затем публичный ключ был передан в виртуальную машину с Debian 12 при
помощи команды:

    ssh-copy-id -p 2222 semenov@127.0.0.1

После ввода пароля ключ был успешно добавлен в `~/.ssh/authorized_keys`
на гостевой системе. Скриншот
[38](#fig:Screenshot_2025-06-19_at_17.19.38.png){reference-type="ref"
reference="fig:Screenshot_2025-06-19_at_17.19.38.png"} демонстрирует
этот процесс.

![Передача ключа с помощью
ssh-copy-id.](img/Screenshot_2025-06-19_at_17.19.38.png){#fig:Screenshot_2025-06-19_at_17.19.38.png
width="90%"}

В завершение была предпринята попытка подключиться к гостевой машине без
ввода пароля:

    ssh -p 2222 semenov@127.0.0.1

Как видно на скриншоте
[39](#fig:Screenshot_2025-06-19_at_17.20.29.png){reference-type="ref"
reference="fig:Screenshot_2025-06-19_at_17.20.29.png"}, подключение
прошло успешно, а пароль больше не требуется.

![Успешное подключение по
ключу.](img/Screenshot_2025-06-19_at_17.20.29.png){#fig:Screenshot_2025-06-19_at_17.20.29.png
width="90%"}

Таким образом, аутентификация по SSH-ключу была успешно настроена, что
позволяет работать с виртуальной машиной быстрее и безопаснее.

## Разработка программного обеспечения

### Установка средств разработки

В рамках раздела **«Разработка программного обеспечения»** была
выполнена установка базового комплекта инструментов для сборки C/C++
программ.

При попытке установить пакет `build-essential` от имени обычного
пользователя возникла ошибка (рис.
[40](#fig:Screenshot_2025-06-19_at_17.22.16.png){reference-type="ref"
reference="fig:Screenshot_2025-06-19_at_17.22.16.png"}).

![Пользователь не имеет прав на установку пакетов и команда `sudo` не
найдена](img/Screenshot_2025-06-19_at_17.22.16.png){#fig:Screenshot_2025-06-19_at_17.22.16.png
width="90%"}

Для решения проблемы была произведена авторизация под суперпользователем
с помощью команды `su`, после чего были обновлены списки пакетов
(`apt update`) и установлен пакет `sudo` (рис.
[41](#fig:VirtualBox_summer_p_semenov_19_06_2025_17_49_52.png){reference-type="ref"
reference="fig:VirtualBox_summer_p_semenov_19_06_2025_17_49_52.png"}).

![Демонстрирует установку
`sudo`.](img/VirtualBox_summer_p_semenov_19_06_2025_17_49_52.png){#fig:VirtualBox_summer_p_semenov_19_06_2025_17_49_52.png
width="90%"}

Затем, чтобы предоставить пользователю `semenov` возможность
использовать `sudo` без необходимости входа под `root`, он был добавлен
в группу `sudo` с помощью команды:

    usermod -aG sudo semenov

После выхода из текущего сеанса и повторного входа пользователь получил
возможность выполнять команды с повышенными правами, используя `sudo`.

Далее была успешно выполнена команда `sudo apt install build-essential`,
как показано на скриншоте
[42](#fig:Screenshot_2025-06-19_at_18.01.20.png){reference-type="ref"
reference="fig:Screenshot_2025-06-19_at_18.01.20.png"}, включающая в
себя компиляторы `gcc`, `g++`, отладочные утилиты, заголовочные файлы и
библиотеки.

![Установка средств
разработки.](img/Screenshot_2025-06-19_at_18.01.20.png){#fig:Screenshot_2025-06-19_at_18.01.20.png
width="90%"}

### Состав пакета `build-essential` {#состав-пакета-build-essential .unnumbered}

Метапакет `build-essential` включает следующий минимальный набор
пакетов, необходимых для сборки программ из исходных кодов на C и C++, а
также для сборки Debian-пакетов:

- `gcc` --- компилятор языка C (GNU Compiler Collection).

- `g++` --- компилятор языка C++ (GNU Compiler Collection).

- `make` --- инструмент для автоматизации сборки проектов на основе
  Makefile.

- `libc6-dev` --- заголовочные файлы стандартной библиотеки C (glibc),
  необходимые для компиляции программ под Linux.

- `libc-dev` --- псевдоним для `libc6-dev`, поддерживаемый для
  совместимости.

- `dpkg-dev` --- пакет, содержащий набор инструментов для создания и
  сопровождения исходных пакетов Debian. В него входят:

  - `dpkg-source` --- программа для распаковки и сборки исходных пакетов
    Debian, управления патчами.

  - `dpkg-gencontrol` --- генерирует файл `DEBIAN/control`, содержащий
    метаданные бинарного пакета на основе информации из исходного
    пакета.

  - `dpkg-shlibdeps` --- анализирует бинарные файлы на предмет
    использования разделяемых библиотек и автоматически вычисляет
    зависимости `Depends`.

  - `dpkg-genchanges` --- формирует файл `.changes`, содержащий сведения
    о построенных пакетах и их проверках, который нужен для загрузки в
    репозиторий.

  - `dpkg-buildpackage` --- основной скрипт для автоматизированной
    сборки бинарных и исходных пакетов Debian из каталога с исходниками.

  - `dpkg-distaddfile` --- добавляет информацию о дополнительных файлах
    в файл `debian/files`, используемый при сборке.

### Использование `sudo` и `apt` для управления пакетами {#использование-sudo-и-apt-для-управления-пакетами .unnumbered}

В операционных системах семейства GNU/Linux, таких как Debian,
управление программным обеспечением чаще всего осуществляется через
менеджер пакетов `APT` (Advanced Package Tool). Он позволяет
устанавливать, обновлять и удалять пакеты программ вместе с их
зависимостями из репозиториев.

Для выполнения подобных действий обычно требуются привилегии
суперпользователя (`root`). Для того чтобы не переключаться в сессию
`root`, используется программа `sudo`, которая позволяет запускать
отдельные команды с правами администратора от имени обычного
пользователя, входящего в группу `sudo`.

Например, команды для установки и обновления пакетов могли выглядеть
следующим образом:

    sudo apt update
    sudo apt install build-essential

где:

- `sudo` --- позволяет запустить команду от имени администратора.

- `apt` --- консольная утилита для работы с пакетами, предоставляющая
  удобный интерфейс для установки, удаления и обновления пакетов.

- `update` --- обновляет локальный кеш списка пакетов из всех
  подключенных репозиториев.

- `install <пакет>` --- устанавливает указанный пакет и все необходимые
  зависимости.

Кроме того, важную роль в управлении пакетами играют зеркала
репозиториев --- специальные серверы, хранящие каталоги пакетов для
выбранного дистрибутива. При установке Debian в VirtualBox в качестве
зеркала был выбран официальный сервер `deb.debian.org`, который
автоматически перенаправляет запросы пользователя на ближайший и
наиболее доступный зеркальный сервер, что повышает скорость загрузки и
устойчивость к сбоям отдельных узлов. При запуске команды
`sudo apt update` утилита `apt` обращается к списку зеркал в файле
`/etc/apt/sources.list`, загружает информацию о доступных версиях
пакетов и обновляет локальный кэш метаданных, позволяя затем быстро
устанавливать или обновлять программное обеспечение из проверенных
источников.

### Анализ зависимостей `/bin/bash`

При помощи команды `ldd /bin/bash` было исследовано, какие динамические
библиотеки используются командным интерпретатором. Затем аналогичная
проверка была выполнена для каждой зависимости рекурсивно, включая
`libtinfo.so.6`, `libc.so.6` и `ld-linux-aarch64.so.1`. Полученное
дерево зависимостей:

<figure id="fig:bash-ldd-tree">

<figcaption>Рекурсивное дерево зависимостей <code>/bin/bash</code>,
построенное с использованием <code>ldd</code></figcaption>
</figure>

Для построения дерева использовалась утилита `ldd`, а также информация,
полученная вручную при проверке каждой библиотеки по отдельности (рис.
[44](#fig:Screenshot_2025-06-26_at_16.20.17.png){reference-type="ref"
reference="fig:Screenshot_2025-06-26_at_16.20.17.png"}).

![рекурсивное исследование зависимостей с помощью
`ldd`.](img/Screenshot_2025-06-26_at_16.20.17.png){#fig:Screenshot_2025-06-26_at_16.20.17.png
width="90%"}

### Анализ зависимостей других программ

С помощью команды `ldd` было также проанализировано количество
зависимостей у основных утилит из п. 3 раздела \<\<Основы работы в
командном интерпретаторе\>\>. Ниже приведено краткое резюме:

- `whoami`, `date`, `cal`, `df`, `cat`, `touch`, `rm`, `whereis` ---
  имеют минимальный набор зависимостей: как правило, это `libc.so.6`,
  загрузчик `ld-linux-aarch64.so.1`, а также виртуальная библиотека
  `linux-vdso.so.1`.

- `hexdump`, `less` --- дополнительно используют библиотеку
  `libtinfo.so.6`, обеспечивающую взаимодействие с терминалом (цвет,
  прокрутка и т.д.).

- `stat`, `find`, `mv`, `cp` --- используют расширенный набор библиотек:
  помимо `libc`, задействованы `libselinux.so.1`, `libacl.so.1`,
  `libattr.so.1`, а также регулярные выражения (`libpcre2-8.so.0`).

- `file` --- одна из самых зависимых утилит, использующая до 7
  динамических библиотек, включая `libmagic`, `libz`, `liblzma`,
  `libbz2`, что связано с определением форматов и сжатыми файлами.

- `grep` --- использует `libpcre2-8.so.0` для поиска по шаблонам.

- `uptime` --- задействует широкую цепочку библиотек: помимо `libproc2`,
  используются `libsystemd`, `libcap`, `liblzma`, `libzstd`, `liblz4`,
  `libgpg-error`, `libgcrypt` и др.

- `time` --- отсутствует как отдельная бинарная утилита в системе,
  возможно реализована как shell built-in или отсутствует в составе
  базового окружения (рис.
  [45](#fig:Screenshot_2025-06-26_at_17.06.14.png){reference-type="ref"
  reference="fig:Screenshot_2025-06-26_at_17.06.14.png"}).

![Вызов `ldd` для
`time`.](img/Screenshot_2025-06-26_at_17.06.14.png){#fig:Screenshot_2025-06-26_at_17.06.14.png
width="90%"}

Количество и состав библиотек может варьироваться в зависимости от
сборки и архитектуры системы (в данном случае --- `aarch64`), однако во
всех случаях ядром зависимостей остаётся стандартная библиотека
`libc.so.6` и динамический загрузчик `ld-linux-aarch64.so.1`.

В качестве подтверждения приведены скриншоты
[46](#fig:Screenshot_2025-06-26_at_16.47.02.png){reference-type="ref"
reference="fig:Screenshot_2025-06-26_at_16.47.02.png"},
[47](#fig:Screenshot_2025-06-26_at_16.47.37.png){reference-type="ref"
reference="fig:Screenshot_2025-06-26_at_16.47.37.png"},
[48](#fig:Screenshot_2025-06-26_at_16.48.18.png){reference-type="ref"
reference="fig:Screenshot_2025-06-26_at_16.48.18.png"},
[49](#fig:Screenshot_2025-06-26_at_16.49.59.png){reference-type="ref"
reference="fig:Screenshot_2025-06-26_at_16.49.59.png"} и
[50](#fig:Screenshot_2025-06-26_at_16.51.37.png){reference-type="ref"
reference="fig:Screenshot_2025-06-26_at_16.51.37.png"} с выводом команды
`ldd` для соответствующих утилит:

![ldd для `hexdump`, `find`,
`whereis`.](img/Screenshot_2025-06-26_at_16.47.02.png){#fig:Screenshot_2025-06-26_at_16.47.02.png
width="90%"}

![ldd для `cat`, `less`,
`grep`.](img/Screenshot_2025-06-26_at_16.47.37.png){#fig:Screenshot_2025-06-26_at_16.47.37.png
width="90%"}

![ldd для `touch`, `rm`, `stat`,
`file`.](img/Screenshot_2025-06-26_at_16.48.18.png){#fig:Screenshot_2025-06-26_at_16.48.18.png
width="90%"}

![ldd для `df`, `cp`,
`mv`.](img/Screenshot_2025-06-26_at_16.49.59.png){#fig:Screenshot_2025-06-26_at_16.49.59.png
width="90%"}

![ldd для `whoami`, `date`,
`uptime`.](img/Screenshot_2025-06-26_at_16.51.37.png){#fig:Screenshot_2025-06-26_at_16.51.37.png
width="90%"}

### Установка  `CMake`, отличие системы сборки и мета-системы

**Система сборки** --- это инструмент, который напрямую управляет
компиляцией и сборкой проекта. Одним из классических примеров является
`make`, использующий файл `Makefile` с набором правил. Пример
простейшего `Makefile`:

    all:
    	$(CC) main.c -o main

**Мета-система сборки**, такая как `CMake`, не выполняет сборку сама по
себе. Она служит для генерации файлов сборки, адаптированных под
конкретную систему или генератор (например, Makefile). Это особенно
полезно при переносе проекта на другие платформы и конфигурации.

Для установки `cmake` была использована команда
`sudo apt install cmake`. Процесс установки придедён на скриншоте
[51](#fig:Screenshot_2025-06-20_at_00.42.27.png){reference-type="ref"
reference="fig:Screenshot_2025-06-20_at_00.42.27.png"}.

![Установка `cmake` и зависимостей через
`apt`.](img/Screenshot_2025-06-20_at_00.42.27.png){#fig:Screenshot_2025-06-20_at_00.42.27.png
width="90%"}

### Описание файла `CMakeLists.txt` {#описание-файла-cmakelists.txt .unnumbered}

Файл `CMakeLists.txt` используется системой CMake для описания правил
сборки проекта. В нём указываются имя проекта, используемый язык
программирования, минимальная версия CMake, а также перечисляются
исходные файлы, из которых нужно собрать библиотеки и исполняемые
программы.

Этот файл позволяет автоматически генерировать Makefile или другие
скрипты сборки, чтобы без ручного ввода команд компилировать исходники,
собирать статические или динамические библиотеки и связывать их с
исполняемыми файлами.

В нашей работе `CMakeLists.txt` применялся для создания библиотек с
функциями поиска максимума, тестовых программ, а также для экспериментов
с флагами оптимизации компилятора. Это делало сборку проекта удобной и
одинаковой на любой платформе, где установлен CMake.

## Задание 1. Реализация библиотеки поиска максимального элемента

В рамках задания была разработана библиотека, реализующая функцию поиска
максимального элемента в массиве. Требовалось реализовать функцию двумя
способами: **итеративным** и **рекурсивным**, без использования
библиотечных функций. Далее библиотека оформлялась в виде статической и
динамической.

### Алгоритмы

На рисунках [52](#fig:iterative){reference-type="ref"
reference="fig:iterative"} и [53](#fig:recursive){reference-type="ref"
reference="fig:recursive"} представлены блок-схемы двух алгоритмов:

- **Итеративный алгоритм**
  (рис. [52](#fig:iterative){reference-type="ref"
  reference="fig:iterative"}): сначала проверяется, является ли размер
  массива неположительным. В этом случае возвращается значение по
  умолчанию (0). Затем инициализируется переменная `max` первым
  элементом массива. После этого выполняется цикл по индексам от 1 до
  `size-1`, в котором каждый элемент сравнивается с текущим максимумом,
  и в случае превышения `max` обновляется.

- **Рекурсивный алгоритм**
  (рис. [53](#fig:recursive){reference-type="ref"
  reference="fig:recursive"}): сначала проверяется, является ли размер
  массива неположительным. В этом случае возвращается значение по
  умолчанию (0). Затем, базовый случай --- массив из одного элемента, в
  этом случае возвращается он. В противном случае производится
  рекурсивный вызов на префикс массива длины `size-1`, затем
  возвращается максимум между этим результатом и последним элементом
  массива.

<figure id="fig:iterative">

<figcaption>Блок-схема итеративного алгоритма поиска максимального
элемента</figcaption>
</figure>

<figure id="fig:recursive">

<figcaption>Блок-схема рекурсивного алгоритма поиска максимального
элемента</figcaption>
</figure>

### Статическая и динамическая библиотеки: понятие и различия

**Статическая библиотека** (файл с расширением `.a`) представляет собой
архив объектных файлов, содержащих реализации функций, которые полностью
встраиваются в исполняемый файл во время компиляции. Это означает, что
при запуске программы весь необходимый код уже содержится внутри
исполняемого файла, и наличие библиотеки на целевой системе не
требуется.

**Динамическая библиотека** (файл с расширением `.so` --- *shared
object*) подключается к программе во время её выполнения (динамически).
Код таких библиотек не копируется в исполняемый файл, а подгружается из
внешнего файла при запуске. Это позволяет:

- уменьшать размер исполняемых файлов;

- обновлять библиотеку без необходимости перекомпиляции программ;

- использовать одну и ту же библиотеку одновременно несколькими
  программами.

### Основные отличия статической и динамической библиотек {#основные-отличия-статической-и-динамической-библиотек .unnumbered}

- Формат файла:

  - Статическая библиотека имеет расширение `.a`;

  - Динамическая библиотека имеет расширение `.so`.

- Связывание:

  - Статическая библиотека встраивается в исполняемый файл во время
    компиляции;

  - Динамическая библиотека подключается при запуске программы.

- Зависимость от внешних файлов:

  - Программа со статической библиотекой не требует внешних файлов при
    запуске;

  - Для программы с динамической библиотекой требуется наличие
    соответствующего `.so`-файла в системе.

- Размер исполняемого файла:

  - Статическая библиотека увеличивает размер исполняемого файла;

  - Динамическая библиотека позволяет сократить размер исполняемого
    файла.

- Обновление и повторное использование:

  - Обновление статической библиотеки требует перекомпиляции программы;

  - Динамическую библиотеку можно обновлять отдельно, без
    перекомпиляции.

### Создание статической и динамической библиотеки {#создание-статической-и-динамической-библиотеки .unnumbered}

Для повторного использования реализованных функций поиска максимального
элемента массива (итеративного и рекурсивного) была создана как
статическая, так и динамическая библиотека.

### 1. Компиляция исходного файла {#компиляция-исходного-файла .unnumbered}

Сначала исходный файл `max.c`, содержащий реализацию функций,
компилируется в объектный файл `max.o`:

    gcc -c max.c

### 2. Создание статической библиотеки {#создание-статической-библиотеки .unnumbered}

Затем объектный файл упаковывается в статическую библиотеку `libmax.a` с
помощью утилиты `ar`:

    ar rcs libmax.a max.o

### 3. Создание динамической библиотеки {#создание-динамической-библиотеки .unnumbered}

Для генерации динамической библиотеки `libmax.so` используется
компилятор `gcc` с флагами `-fPIC` (положение-независимый код) и
`-shared`:

    gcc -fPIC -shared -o libmax.so max.o

### 4. Содержимое рабочей директории {#содержимое-рабочей-директории .unnumbered}

На скриншоте
[54](#fig:Screenshot_2025-06-20_at_01.10.14.png){reference-type="ref"
reference="fig:Screenshot_2025-06-20_at_01.10.14.png"} представлено
содержимое директории после выполнения всех шагов:

![Создание статической (`libmax.a`) и динамической (`libmax.so`)
библиотек](img/Screenshot_2025-06-20_at_01.10.14.png){#fig:Screenshot_2025-06-20_at_01.10.14.png
width="90%"}

результате сборки в папке находятся:

- `max.c`, `max.h` --- исходные файлы;

- `max.o` --- объектный файл;

- `libmax.a` --- статическая библиотека;

- `libmax.so` --- динамическая библиотека.

Таким образом, была успешно реализована и собрана библиотека поиска
максимального элемента в массиве. Поддерживаются три типа данных и две
стратегии реализации. Библиотека подготовлена как в виде статической,
так и динамической и сопровождается блок-схемами, отражающими логику
работы каждого варианта.

### 5. Тестирование, компиляция и эксперименты {#тестирование-компиляция-и-эксперименты .unnumbered}

Все материалы, относящиеся к тестированию работы реализованной
библиотеки, включая исходные коды, файлы сборки и скрипты тестирования,
размещены в разделе **«Результаты работы и эксперименты»**.

данном разделе находятся:

- тестирующая программа, использующая реализацию поиска максимального
  элемента;

- файл `CMakeLists.txt`, позволяющий собрать программу со статической
  или динамической линковкой;

- скрипты для запуска экспериментов с различными флагами оптимизации
  компилятора (`-O0`, `-O1`, `-O2`, `-Os`);

- результаты, полученные с помощью утилиты `time`, фиксирующей реальное
  время выполнения;

- модифицированная версия тестирующей программы, использующая `dlopen`
  для загрузки библиотеки во время выполнения;

- сравнение подходов: статическая линковка, динамическая линковка и
  загрузка через `dlopen`;

- анализ разделяемых библиотек с помощью команды `ldd` и интерпретация
  полученного вывода.

## Задание 2. Двоичные файлы и визуализация структур в псевдографике

### Описание структуры из индивидуального варианта

В рамках данного задания была составлена структура для хранения
информации о человеке, включающая следующие поля:

- **Фамилия и инициалы** --- строка фиксированной длины (до 64
  символов),

- **Знак зодиака** --- строка фиксированной длины (до 32 символов),

- **Год рождения** --- целое число, представляющее год.

Редактирование исходного кода структуры производилось в текстовом
редакторе `nano` на гостевой системе через SSH-сессию.

### Описание заголовка базы данных

Также была составлена структура заголовка бинарного файла базы данных.
Она содержит следующие поля:

- **Сигнатура формата файла** --- первые 4 байта файла, в которые
  записывается "SEME" (по первым буквам фамилии Semenov). Это позволяет
  в дальнейшем при чтении файла проверить, соответствует ли файл
  ожидаемому формату.

- **Номер транзакции** (`transaction_id`) --- целое число (4 байта),
  которое увеличивается при каждом обращении к файлу, как при чтении,
  так и при записи. Это даёт возможность отследить количество операций с
  файлом и потенциальные изменения данных.

- **Количество записей** (`count`) --- целое число (4 байта),
  указывающее сколько структур ZNAK находится в данном файле после
  заголовка.

- **Контрольная сумма (CRC-32)** --- вычисляется по алгоритму CRC-32 для
  всех данных, следующих за заголовком. Она нужна для того, чтобы в
  любой момент можно было проверить целостность данных: если файл будет
  повреждён или модифицирован некорректно, пересчитанная контрольная
  сумма не совпадёт с записанной, и можно будет определить, что данные
  ненадёжны.

Редактирование исходного файла, содержащего как структуру данных о
человеке, так и структуру заголовка базы данных, производилось в
редакторе `nano`.

Таким образом, заголовок обеспечивает:

- идентификацию формата файла с помощью сигнатуры;

- учёт количества операций чтения/записи через transaction_id;

- хранение информации о количестве записей в базе;

- проверку целостности данных с помощью CRC-32.

### Использование `uint32_t` вместо `int` {#использование-uint32_t-вместо-int .unnumbered}

Для описания полей заголовка базы данных использовался `uint32_t` из
заголовочного файла `<stdint.h>`, а не привычный `int`. Это сделано по
следующим причинам:

- **Гарантированный размер:** Тип `uint32_t` представляет собой
  **беззнаковое целое ровно 4 байта (32 бита)** на всех платформах. В то
  время как `int` в языке C лишь гарантирует, что будет не менее 2 байт
  (обычно 4 байта, но это зависит от архитектуры и компилятора).

- **Переносимость бинарного формата:** Так как база данных сохраняется в
  бинарном формате, важно, чтобы структура файла выглядела одинаково на
  всех системах (например, x86, ARM). Использование `uint32_t` позволяет
  гарантировать, что поля заголовка будут точно занимать 4 байта, а
  значит файл можно будет прочитать на любой платформе.

- **Отсутствие знака для идентификаторов и счётчиков:** Поля
  `transaction_id`, `count` и `checksum` не могут быть отрицательными по
  своей природе. Использование беззнакового типа `uint32_t` явно
  показывает, что эти значения не должны быть отрицательными, и
  исключает возможность их интерпретации как отрицательных чисел.

### Реализация программы с использованием ncurses для работы с бинарной базой данных

В рамках данного задания была составлена программа на языке `C`,
использующая библиотеку `ncurses` для визуализации работы с двоичным
файлом базы данных. Программа включает:

- интерфейс для ввода и отображения записей;

- хранение данных в бинарном файле со специальным заголовком;

- выполнение индивидуального задания --- поиск по знаку зодиака и году
  рождения;

- возможность добавления записей с обновлением заголовка.

### Использование библиотеки `ncurses` и `zlib` {#использование-библиотеки-ncurses-и-zlib .unnumbered}

При реализации программы использовались две важные внешние библиотеки:

- **`ncurses`** --- для организации псевдографического интерфейса в
  терминале;

- **`zlib`** --- для вычисления контрольной суммы данных в формате
  CRC-32.

### Работа с библиотекой `ncurses`

Библиотека `ncurses` предоставляет набор функций для работы с текстовым
интерфейсом в терминале, включая управление выводом текста, перемещением
курсора, очисткой экрана и считыванием пользовательского ввода без
перехода на новую строку.

В программе использовались следующие функции:

- `initscr()`, `endwin()` --- инициализация и завершение работы с
  экраном;

- `cbreak()`, `noecho()`, `echo()` --- настройка режима ввода (без
  ожидания Enter, а также включение/отключение отображения вводимых
  символов на экране; например, `noecho()` используется для того, чтобы
  не выводить вводимые пользователем символы непосредственно в
  терминале, а выводить их вручную через `mvprintw()`);

- `setlocale(LC_ALL, "ru_RU.UTF-8")` --- установка локали для
  корректного вывода и обработки кириллицы в интерфейсе (поддержка UTF-8
  для псевдографики и русских символов);

- `clear()`, `move()`, `clrtoeol()` --- очистка всего экрана или
  отдельных строк;

- `mvprintw()` --- вывод форматированного текста в заданную позицию;

- `getnstr()`, `scanw()`, `getch()` --- считывание строк, чисел и
  одиночных символов от пользователя;

- `getmaxyx()` --- получение размеров терминала для реализации
  постраничного вывода.

Благодаря `ncurses` был реализован удобный интерфейс с полями ввода,
сообщениями об ошибках и навигацией между страницами при отображении
большого количества записей в базе данных.

### Использование библиотеки `zlib`

Библиотека `zlib` использовалась для расчёта контрольной суммы всех
данных, хранящихся в файле базы. Это позволяет в дальнейшем проверить
целостность базы данных, так как любое повреждение файла изменит
значение CRC-32.

Программа применяет следующие функции из `zlib.h`:

- `crc32(0L, Z_NULL, 0)` --- инициализация значения контрольной суммы;

- `crc32(previous_crc, (const Bytef*)data, size)` --- вычисление CRC-32
  для заданного блока данных.

Контрольная сумма записывается в заголовок базы данных и обновляется при
каждом изменении данных, например, при добавлении новой записи.

Чтобы воспользоваться библиотекой `zlib`, необходимо выполнить несколько
шагов:

- **Установить саму библиотеку и её заголовочные файлы.** Для
  Debian/Ubuntu это делается командой:

      sudo apt install zlib1g-dev

  Пакет `zlib1g-dev` содержит как бинарную динамическую библиотеку
  (`libz.so`), так и файлы для компиляции (`zlib.h`).

- **Подключить заголовочный файл в исходном коде программы:**

      #include <zlib.h>

- **При сборке с помощью gcc добавить флаг линковки `-lz`**, чтобы
  компоновщик подключил библиотеку `zlib`:

      gcc main.c -lz -o app

  Или в `CMakeLists` можно добавить:

      find_package(ZLIB REQUIRED)
      target_link_libraries(myapp ZLIB::ZLIB)

  Тогда `CMake` сам подставит правильные пути к библиотекам и
  заголовкам.

### Исходные файлы программы

Программа разбита на несколько модулей:

- `znak.h` --- описание структур данных;

- `app.h` --- объявления функций работы с интерфейсом и файлами;

- `app.c` --- реализация функций интерфейса, работы с файлом и структуры
  базы;

- `main.c` --- точка входа и главное меню.

### Файл `znak.h` {#файл-znak.h .unnumbered}

Заголовочный файл, где объявляются все поля структур.

### Файл `app.h` {#файл-app.h .unnumbered}

Заголовочный файл с объявлениями всех функций и необходимых констант ---
В этом заголовочном файле задекларированы все функции программы,
необходимые для работы с ncurses и двоичным файлом базы данных. Приведём
краткое описание каждой функции:

- `save_db(ZNAK_DB_HEADER *header, ZNAK *arr, int count)` --- сохраняет
  в бинарный файл заголовок и массив структур `ZNAK`, обновляя
  контрольную сумму и увеличивая номер транзакции при каждом сохранении.

- `find_by_year_and_zodiac(ZNAK_DB_HEADER *header, ZNAK *arr, int count, const char *zodiac, int year)`
  --- осуществляет поиск по базе данных по заданным году рождения и
  знаку зодиака. Отображает найденные результаты в виде таблицы на
  экране.

- `add_record(ZNAK_DB_HEADER *header, ZNAK *arr, int *count)` ---
  добавляет новую запись в массив, корректирует заголовок (увеличивает
  количество записей и номер транзакции), после чего сразу сохраняет
  базу на диск.

- `search_records(ZNAK_DB_HEADER *header, ZNAK *arr, int count)` ---
  интерфейс поиска: запрашивает у пользователя необходимые параметры
  (год и знак зодака), после чего вызывает функцию
  `find_by_year_and_zodiac` для выполнения поиска.

- `print_db_header(ZNAK_DB_HEADER *header)` --- выводит информацию о
  заголовке базы данных: сигнатуру, номер транзакции, число записей и
  CRC-32 контрольную сумму.

- `db_print_interface(ZNAK_DB_HEADER *header, ZNAK *arr, int count)` ---
  основной интерфейс для просмотра всех записей в базе с постраничным
  выводом и управлением навигацией через клавиши `n`, `p`, `q`.

- `print_db(ZNAK_DB_HEADER *header, ZNAK *arr, int count)` ---
  вспомогательная функция, используемая для табличного вывода всех
  записей без информации о заголовке.

- `error_msg(const char *msg)` --- показывает сообщение об ошибке в
  ncurses-окне и ждёт нажатия клавиши для продолжения работы.

- `clear_except_first_line()` --- очищает весь экран кроме первой
  строки, что позволяет аккуратно обновлять интерфейс, сохраняя
  статусную строку.

### Файл `app.c` {#файл-app.c .unnumbered}

Ниже представлена реализация всех функций, обеспечивающих работу
программы с использованием библиотеки `ncurses` и двоичного файла базы
данных, на псевдокоде. Здесь сосредоточена вся прикладная логика,
включая интерфейс ввода/вывода, работу с заголовком, чтение и запись
файла, обработку ошибок и механизм постраничного отображения.

### Вывод сообщений об ошибках {#вывод-сообщений-об-ошибках .unnumbered}

Функция `error_msg` предназначена для отображения сообщений об ошибках в
интерфейсе `ncurses`. Она очищает экран, выводит переданное ей текстовое
сообщение на первой строке и подсказку для пользователя на третьей
строке о необходимости нажать любую клавишу для продолжения. После
нажатия клавиши экран снова очищается, позволяя вернуться к нормальному
взаимодействию программы.

``` {.objectivec language="C" caption="Функция error\\_msg"}
функция error_msg(строка msg):
  очистить экран
  вывести строку msg на экран в позиции (1, 2)
  вывести строку в позиции (3, 2):
    "Нажмите любую клавишу для повторной попытки..."
  ожидать нажатия любой клавиши
  снова очистить экран
конец функции
```

### Функция ввода записи {#функция-ввода-записи .unnumbered}

Функция `input_znak` обеспечивает пошаговый ввод пользователем данных
для полей структуры `ZNAK`: фамилии и инициалов, знака зодиака и года
рождения, с использованием интерфейса `ncurses`. Для строковых полей
предусмотрены проверки на пустой ввод, а для года рождения --- проверка
диапазона от 1899 до 2025. Все данные сразу записываются в
соответствующие поля структуры.

``` {.objectivec language="C" caption="Функция input\\_znak"}
функция input_znak(указатель z):
  включить режим отображения вводимых символов (echo)

  повторять
    вывести строку "Фамилия и инициалы: " в позиции (2, 2)
    считать строку и сохранить в z.name
    если длина z.name > 0
      выйти из цикла
    иначе
      вызвать error_msg("Фамилия и инициалы не могут быть пустыми.")
  пока не будет введена непустая строка

  повторять
      вывести строку "Знак зодиака: " в позиции (3, 2)
      считать строку и сохранить в z.zodiac
      если длина z.zodiac > 0
        выйти из цикла
      иначе
        вызвать error_msg("Знак зодиака не может быть пустым.")
  пока не будет введена непустая строка

  повторять
    вывести строку "Год рождения: " в позиции (4, 2)
    считать целое число в переменную birth_year
    если birth_year в диапазоне [1899, 2025]
      присвоить z.birth_year <- birth_year
      выйти из цикла
    иначе
      вызвать error_msg("Год рождения должен быть в диапазоне 1899-2025.")
  пока не будет введён корректный год

  отключить режим отображения вводимых символов (noecho)
конец функции
```

### Сохранение базы в файл {#сохранение-базы-в-файл .unnumbered}

Функция `save_db` предназначена для сохранения текущего состояния базы
данных в бинарный файл. Сначала открывается файл для записи в бинарном
режиме. Затем обновляется поле `count` заголовка, инкрементируется
`transaction_id`, и вычисляется контрольная сумма CRC32 по всем данным
массива структур для последующей проверки целостности. После этого в
файл последовательно записываются заголовок и массив структур. В
завершение файл закрывается.

``` {.objectivec language="C" caption="Функция save\\_db"}
функция save_db(header, arr, count):
  открыть файл DB_FILENAME для записи в бинарном режиме и 
  присвоить дескриптор file
  если file == NULL тогда
    вывести сообщение об ошибке "Ошибка создания файла"
    завершить функцию

  header.count присвоить count
  увеличить header.transaction_id на 1

  вычислить CRC32 для пустого буфера и записать в header.checksum
  вычислить CRC32 для массива arr длиной sizeof(ZNAK) * count
    используя предыдущий header.checksum как начальное значение
    и обновить header.checksum

  записать структуру header в файл (fwrite)
  записать массив структур arr в файл (fwrite)
  закрыть файл (fclose)
конец функции
```

### Добавление новой записи в базу данных {#добавление-новой-записи-в-базу-данных .unnumbered}

Функция `add_record` реализует режим добавления одной новой записи в
массив структур. При вызове она очищает экран, выводит заголовок с
сообщением о начале добавления записи, затем с помощью функции
`input_znak` запрашивает у пользователя данные для новой структуры.
После успешного ввода увеличивает счётчик элементов массива и сразу
сохраняет обновлённую базу данных в файл с помощью функции `save_db`.

``` {.objectivec language="C" caption="Функция add\\_record"}
функция add_record(header, arr, count):
  очистить экран
  вывести строку "Добавление новой записи:" в позицию (1, 2)

  вызвать функцию input_znak и передать ей элемент массива arr по 
  индексу *count
  увеличить *count на 1

  вызвать функцию save_db с параметрами header, arr и *count
конец функции
```

### Вывод заголовка базы данных {#вывод-заголовка-базы-данных .unnumbered}

Данная функция отвечает за отображение основной служебной информации о
бинарной базе данных на экран. Она выводит сигнатуру, номер транзакции,
количество записей в базе и контрольную сумму CRC32, обеспечивающую
целостность данных. После вывода всех полей заголовка пользователю
предлагается нажать любую клавишу для продолжения.

``` {.objectivec language="C" caption="Функция print\\_db\\_header"}
функция print_db_header(header):
    очистить экран

    вывести строку "Заголовок базы данных:" в позицию (1, 2)
    вывести строку "Сигнатура: ..."         в позицию (2, 4),
    подставив header.signature

    вывести строку "Номер транзакции: ..."  в позицию (3, 4), 
    подставив header.transaction_id

    вывести строку "Количество записей: ..." в позицию (4, 4), 
    подставив header.count

    вывести строку "Контрольная сумма (CRC32): ..." в позицию (5, 4),
    подставив header.checksum

    вывести строку "Нажмите любую клавишу для выхода..." в позицию (20, 2)
    ожидать нажатие клавиши с помощью getch()
конец функции
```

### Очистка экрана за исключением первой строки {#очистка-экрана-за-исключением-первой-строки .unnumbered}

Функция `clear_except_first_line` используется для очистки терминала с
помощью библиотеки `ncurses`, оставляя нетронутой первую строку экрана.
Это достигается за счёт прохода в цикле по строкам начиная со второй и
вызова `clrtoeol` для стирания содержимого каждой строки. Таким образом
обеспечивается возможность сохранения заголовка или другого важного
сообщения в верхней части экрана при обновлении основного содержимого.

``` {.objectivec language="C" caption="Функция clear\\_except\\_first\\_line"}
функция clear_except_first_line():
  получить размеры экрана (rows, cols) с помощью getmaxyx

  для i от 2 до rows:
    переместить курсор в начало строки i (позиция (i, 0))
    очистить строку до конца с помощью clrtoeol
конец функции
```

### Интерфейс просмотра базы данных с постраничным выводом {#интерфейс-просмотра-базы-данных-с-постраничным-выводом .unnumbered}

Функция `db_print_interface` организует интерфейс просмотра содержимого
базы данных в виде таблицы с постраничной навигацией. С помощью
возможностей библиотеки `ncurses` она отрисовывает заголовки столбцов
(«№», «Фамилия и инициалы», «Знак зодиака», «Год») и строки таблицы, где
каждая строка соответствует одной записи структуры `ZNAK`. Вывод делится
на страницы, количество записей на странице зависит от размеров текущего
окна терминала. Пользователь может листать данные с помощью клавиш `n`
(следующая страница) и `p` (предыдущая страница), либо выйти, нажав `q`.

``` {.objectivec language="C" caption="Функция db\\_print\\_interface"}
функция db_print_interface(header, arr, count):
  получить размеры экрана (rows, cols) с помощью getmaxyx

  lines_per_page := rows - 10
  start := 0

  пока start < count:
    очистить все строки, кроме первой с помощью clear_except_first_line

    вывести заголовок таблицы на экран:
      строка 2: "№  Фамилия и инициалы  Знак зодиака  Год"
    нарисовать горизонтальную линию в строке 3

    row := 4
    для i от start до min(count, start + lines_per_page) - 1:
      вывести запись с номером (i+1), 
      фамилией, знаком зодиака и годом рождения в строку row
      row := row + 1

    вывести в нижнюю строку (rows - 2):
      "Страница X/Y. Нажмите 'n' для следующей, 
'p' для предыдущей, 'q' для выхода."

    считать клавишу ch
    если ch == 'q' или ch == 'Q':
      выйти из цикла
    иначе если ch == 'n' или ch == 'N':
      start := start + lines_per_page
    иначе если ch == 'p' или ch == 'P' и start - lines_per_page >= 0:
      start := start - lines_per_page
    иначе:
      выйти из цикла
конец функции
```

### Вывод содержимого базы данных {#вывод-содержимого-базы-данных .unnumbered}

Функция `print_db` используется для вывода всех записей базы данных на
экран. Сначала она очищает экран и вызывает `save_db` для актуализации
заголовка и пересчёта контрольной суммы. Затем выводит заголовок с
сообщением `"Содержимое базы данных:"` и передаёт управление функции
`db_print_interface`, которая обеспечивает постраничную навигацию и
форматированный вывод записей.

``` {.objectivec language="C" caption="Функция print\\_db"}
функция print_db(header, arr, count):
  очистить весь экран

  вызвать функцию save_db(header, arr, count)
  // сохраняет текущее состояние базы данных в файл
  // обновляет номер транзакции и контрольную сумму

  вывести строку в позицию (1,2):
    "Содержимое базы данных:"

  вызвать функцию db_print_interface(header, arr, count)
  // запускает постраничный интерфейс просмотра записей

конец функции
```

### Поиск по знаку зодиака и году {#поиск-по-знаку-зодиака-и-году .unnumbered}

Функция `find_by_year_and_zodiac` выполняет поиск записей в базе данных
по указанному знаку зодиака и году рождения. Сначала она вызывает
`save_db`, чтобы обновить заголовок и пересчитать контрольную сумму
базы. Затем формирует временный массив найденных записей, проходя по
всем данным и сравнивая значения. Если совпадения есть, вызывает
`db_print_interface` для постраничного вывода результатов. Если
совпадений не найдено, выводит соответствующее сообщение и ждёт нажатия
клавиши для возврата в меню.

``` {.objectivec language="C" caption="Функция find\\_by\\_year\\_and\\_zodiac"}
функция find_by_year_and_zodiac(header, arr, count, zodiac, year):
  вызвать save_db(header, arr, count)
  // сохраняет актуальное состояние базы

  found := 0
  создать пустой массив found_arr размером count

  для i от 0 до count - 1:
    если arr[i].birth_year == year и arr[i].zodiac == zodiac:
      found_arr[found] := arr[i]
      found := found + 1

  очистить весь экран
  вывести строку на (1,2):
    "Результаты поиска по знаку 'zodiac' и году year:"

  если found > 0:
    вызвать db_print_interface(header, found_arr, found)
    // покажет найденные записи в интерфейсе
  иначе:
    вывести строку на (5,4):
      "Совпадений не найдено."
    вывести строку на (20,2):
      "Нажмите любую клавишу для выхода..."
    ожидать нажатия клавиши

конец функции
```

### Запрос параметров для поиска {#запрос-параметров-для-поиска .unnumbered}

Функция `search_records` отвечает за интерфейс ввода пользователем
параметров поиска. Она очищает экран, запрашивает у пользователя знак
зодиака и год рождения с помощью `getnstr` и `scanw`. После этого
передаёт введённые данные в функцию `find_by_year_and_zodiac`, которая
непосредственно выполняет поиск и выводит результаты.

``` {.objectivec language="C" caption="Функция search\\_records"}
функция search_records(header, arr, count):
  создать строку search_zodiac
  создать целое search_year

  очистить весь экран
  вывести строку на (1,2):
      "Введите знак зодиака: "
  включить отображение ввода
  считать строку в search_zodiac
  отключить отображение ввода

  вывести строку на (2,2):
      "Введите год рождения: "
  включить отображение ввода
  считать число в search_year
  отключить отображение ввода

  вызвать find_by_year_and_zodiac(header, arr, count, search_zodiac, search_year)
  // выполняет поиск и выводит результаты через интерфейс

конец функции
```

### Файл `main.c` {#файл-main.c .unnumbered}

Главная управляющая функция программы расположена в файле `main.c`.
Здесь выполняется инициализация терминала через библиотеку `ncurses`,
настройка локали для поддержки кириллицы и псевдографики, а также
реализован основной цикл взаимодействия с пользователем. Программа
начинает свою работу с попытки открыть бинарный файл базы данных. Если
файл отсутствует, пользователю предлагается ввести количество записей
для создания новой базы, после чего последовательно вводятся данные о
людях (фамилия и инициалы, знак зодиака и год рождения), которые сразу
же сохраняются в файл. При наличии файла база загружается в оперативную
память, а пользователь сразу переходит к главному меню программы.

Основной цикл предоставляет следующие возможности:

- Просмотр заголовка базы данных (сигнатура, номер транзакции,
  количество записей и контрольная сумма CRC32).

- Просмотр всех записей базы данных с постраничной навигацией.

- Поиск записей по году рождения и знаку зодиака с последующим выводом
  результатов.

- Добавление новой записи в базу данных.

- Выход из программы.

Завершая работу, программа корректно завершает режим работы `ncurses`
через `endwin()`, что восстанавливает стандартное состояние терминала.

Для наглядности интерфейса приведены скриншоты
[55](#fig:Screenshot_2025-07-01_at_13.45.33.png){reference-type="ref"
reference="fig:Screenshot_2025-07-01_at_13.45.33.png"} и
[56](#fig:Screenshot_2025-07-01_at_13.46.15.png){reference-type="ref"
reference="fig:Screenshot_2025-07-01_at_13.46.15.png"}, иллюстрирующие
работу программы:

![Главное меню программы с выбором действия. Пользователю предлагаются
опции для просмотра заголовка базы, просмотра всех записей, поиска по
критериям, добавления записи или выхода из
программы.](img/Screenshot_2025-07-01_at_13.45.33.png){#fig:Screenshot_2025-07-01_at_13.45.33.png
width="90%"}

![Сообщение о том, что файл базы данных не найден. В этом случае
пользователь должен указать количество записей для создания новой базы,
после чего выполняется последовательный ввод
данных.](img/Screenshot_2025-07-01_at_13.46.15.png){#fig:Screenshot_2025-07-01_at_13.46.15.png
width="90%"}

### Замечание о сборке, тестировании и исследовании зависимостей {#замечание-о-сборке-тестировании-и-исследовании-зависимостей .unnumbered}

Сборочные файлы, тестирующие программы, а также все эксперименты по
запуску и исследованию работы разработанной программы находятся в
отдельном разделе **«Результаты работы и эксперименты»**.

В данном разделе будут приведены:

- подробное описание сборки программы с использованием `CMakeLists.txt`;

- результаты тестирования работы приложения с различными наборами
  данных;

- исследование разделяемых библиотек в полученной программе с помощью
  утилиты `ldd`;

- построение рекурсивного дерева зависимостей на основе результатов
  работы `ldd`.

# Результаты работы и эксперименты

## Тестирование и компиляция библиотеки поиска максимального элемента

Для проверки корректности работы библиотеки поиска максимального
элемента был создан каталог `tests`, в котором размещён файл `test.c`.

Ниже приводится краткое описание функций, использованных в тестовой
программе:

- `int myrand()` --- простая реализация генератора псевдослучайных
  чисел. Использует фиксированный `seed`, что позволяет при каждом
  запуске получать одну и ту же последовательность случайных чисел. Это
  важно для корректного сравнения времени работы функций, так как они
  получают одинаковые данные на вход.

  - `int myrand()`

    - Использует глобальную переменную `seed`.

    - Каждый раз перемножает `seed` на константу, добавляет другую
      константу, затем сдвигает и берёт маску.

    - Возвращает «случайное» число от 0 до $2^{15}-1$.

    - Благодаря фиксированному начальному `seed` при каждом запуске даёт
      одинаковую последовательность чисел.

- `double measure(int (*func)(const int*, int), const int* arr, int size)`
  --- функция измерения времени выполнения. Принимает указатель на
  функцию поиска максимума (`max_iterative` или `max_recursive`), массив
  и его размер.

  - С помощью `gettimeofday` замеряется время до и после вызова функции
    поиска максимального элемента.

  - Вычисляется затраченное время в миллисекундах, выводится результат
    (максимальный элемент) и это время на экран.

  - Возвращает затраченное время в качестве значения типа `double`.

- `int main()` --- главная функция тестовой программы.

  - Создаёт массив из $100\,000$ случайных целых чисел с помощью функции
    `myrand`.

  - Поочерёдно вызывает функцию `measure` для двух вариантов поиска
    максимального элемента: `max_iterative` и `max_recursive`.

  - Таким образом, обеспечивает единообразное тестирование и вывод
    времени работы обоих алгоритмов на одном и том же массиве.

**Замечание.** Тестирование было проведено только для типа `int`, так
как написание отдельных тестовых программ для типов `char` и `double` не
представлялось целесообразным --- проверка общих свойств алгоритмов
(итеративности и рекурсии) и их времени работы в достаточной степени
демонстрируется на примере целых чисел.

### Сборка статической и динамической библиотек

Для автоматизации процесса компиляции и линковки проекта был создан
скрипт `CMakeLists.txt`, который редактировался в редакторе `nano`. В
этом скрипте задаётся сборка библиотеки поиска максимального элемента в
двух вариантах:

- **Статическая библиотека** (`libmax.a`), собираемая через:

      add_library(max_static STATIC ${MAX_SRC} ${MAX_HDR})

- **Динамическая библиотека** (`libmax.so`), через:

      add_library(max_shared SHARED ${MAX_SRC} ${MAX_HDR})

Также описываются два исполняемых файла для тестирования этих библиотек:

- `test_static` --- программа, линкующаяся со статической версией
  библиотеки;

- `test_dynamic` --- программа для динамической линковки.

Дополнительно используется установка свойств `BUILD_RPATH` и
`INSTALL_RPATH` для `test_dynamic`, чтобы исполняемый файл мог находить
`libmax.so` в той же папке без дополнительных переменных окружения.

приведено описание работы CMakeLists.txt на псевдокоде:

- Устанавливается минимальная версия CMake: `3.10`.

- Задаётся проект с именем `MaxLibTest` на языке С.

- Устанавливается стандарт языка С на C99.

- Определяются переменные:

  - `MAX_SRC` = `max.c` (исходный файл с реализацией функций)

  - `MAX_HDR` = `max.h` (заголовочный файл с объявлениями)

- Создаётся статическая библиотека:

  - `add_library(max_static STATIC ...)`

  - Присваивается ей имя выходного файла `libmax.a` (через
    `OUTPUT_NAME max`)

- Создаётся динамическая библиотека:

  - `add_library(max_shared SHARED ...)`

  - Присваивается имя выходного файла `libmax.so` (также
    `OUTPUT_NAME max`)

- Добавляется директория с заголовочными файлами в пути для компиляции.

- Создаётся исполняемый файл `test_static`, который компилируется из
  `tests/test.c` и связывается со статической библиотекой `max_static`.

- Создаётся исполняемый файл `test_dynamic`, который компилируется из
  того же `tests/test.c`, но связывается с динамической библиотекой
  `max_shared`.

- Для `test_dynamic` устанавливаются свойства `BUILD_RPATH` и
  `INSTALL_RPATH`, чтобы исполняемый файл во время выполнения
  автоматически находил `libmax.so` в каталоге сборки.

подготовки `CMakeLists.txt` проект собирался следующими командами:

    mkdir build
    cd build
    cmake ..
    make

На скриншоте
[57](#fig:Screenshot_2025-06-30_at_15.21.29.png){reference-type="ref"
reference="fig:Screenshot_2025-06-30_at_15.21.29.png"} показан процесс
сборки с помощью `CMake` и `Make`:

![Процесс сборки проекта с созданием библиотек и исполняемых
файлов.](img/Screenshot_2025-06-30_at_15.21.29.png){#fig:Screenshot_2025-06-30_at_15.21.29.png
width="90%"}

В результате в каталоге `build` появились файлы:

- `libmax.a` --- статическая библиотека;

- `libmax.so` --- динамическая библиотека;

- `test_static` --- программа для теста со статической линковкой;

- `test_dynamic` --- программа для теста с динамической линковкой.

#### Флаг компиляции

Сборка происходила с использованием уровня оптимизации по умолчанию для
CMake. На представленном скриншоте `CMakeLists.txt` отсутствует явный
флаг `-O2`, значит компиляция прошла с флагом по умолчанию (обычно `-O0`
или `-O2` в зависимости от предустановок CMake/компилятора).

Более подробно влияние флагов компиляции будет рассматриваться в
следующем пункте.

#### Тестирование

На скриншоте
[58](#fig:Screenshot_2025-06-30_at_15.23.01.png){reference-type="ref"
reference="fig:Screenshot_2025-06-30_at_15.23.01.png"} показан запуск
тестовых программ и вывод времени работы для статической и динамической
версий:

![Результаты запуска программ test_dynamic и
test_static.](img/Screenshot_2025-06-30_at_15.23.01.png){#fig:Screenshot_2025-06-30_at_15.23.01.png
width="90%"}

Видно, что для обоих случаев итеративная версия поиска максимального
элемента работает значительно быстрее рекурсивной. При этом результаты
одинаковы, что подтверждает корректность реализации.

### Сборка с различными флагами оптимизации

Для исследования влияния оптимизаций компилятора на производительность
программы был составлен скрипт `test.bash`.

приведено его описание на псевдокоде:

- Для каждого оптимизационного флага компилятора `-O0`, `-O1`, `-O2`,
  `-O3`, `-Os` выполнить следующие шаги:

  1.  Вывести на экран сообщение: `"=== Building with -O... ==="`, где
      вместо `...` подставляется текущий флаг.

  2.  Запустить CMake с установкой переменной `CMAKE_C_FLAGS` равной
      текущему флагу оптимизации и собрать файлы в отдельный каталог
      сборки `build_O...`, соответствующий флагу.

  3.  Затем выполнить команду сборки в этом каталоге (аналогично
      `make`).

Этот скрипт автоматизирует процесс конфигурации и сборки проекта с
различными значениями флага `-O`, проходя по ним в цикле. В результате
для каждого уровня оптимизации создаётся отдельный каталог (`build_O0`,
`build_O1` и т.д.), где собираются две версии программы: со статической
и динамической линковкой.

На скриншотах
[59](#fig:Screenshot_2025-06-30_at_15.48.38.png){reference-type="ref"
reference="fig:Screenshot_2025-06-30_at_15.48.38.png"},
[60](#fig:Screenshot_2025-06-30_at_16.05.46.png){reference-type="ref"
reference="fig:Screenshot_2025-06-30_at_16.05.46.png"},
[61](#fig:Screenshot_2025-06-30_at_16.06.23.png){reference-type="ref"
reference="fig:Screenshot_2025-06-30_at_16.06.23.png"} и
[62](#fig:Screenshot_2025-06-30_at_16.07.15.png){reference-type="ref"
reference="fig:Screenshot_2025-06-30_at_16.07.15.png"} показан вывод
выполнения скрипта и запуск тестов для каждого уровня оптимизации:

![Запуск скрипта
test.bash.](img/Screenshot_2025-06-30_at_15.48.38.png){#fig:Screenshot_2025-06-30_at_15.48.38.png
width="90%"}

![Запуск тестов для статической и динамической линковки с `-O0` и
`-O1`.](img/Screenshot_2025-06-30_at_16.05.46.png){#fig:Screenshot_2025-06-30_at_16.05.46.png
width="90%"}

![Запуск тестов для `-O2`,
`-O3`.](img/Screenshot_2025-06-30_at_16.06.23.png){#fig:Screenshot_2025-06-30_at_16.06.23.png
width="90%"}

![Запуск тестов для
`-Os`.](img/Screenshot_2025-06-30_at_16.07.15.png){#fig:Screenshot_2025-06-30_at_16.07.15.png
width="90%"}

### Оптимизация компиляции и её влияние

- `-O0` --- оптимизации отключены. Компилятор генерирует максимально
  «прямолинейный» и понятный код, близкий к исходному, что упрощает
  отладку, но замедляет исполнение.

- `-O1` --- базовые оптимизации. Производятся простые преобразования
  кода для уменьшения размера и ускорения выполнения.

- `-O2` --- более агрессивные оптимизации, включая развёртывание циклов,
  устранение мёртвого кода и др. Обычно используется по умолчанию для
  сборки релизов.

- `-O3` --- включает все оптимизации `-O2` плюс дополнительные
  агрессивные, что может ускорить некоторые программы, но иногда
  приводит к большему размеру бинарного файла и непредсказуемым побочным
  эффектам.

- `-Os` --- особый режим, при котором компилятор старается уменьшить
  размер исполняемого файла, при этом сохраняя большую часть оптимизаций
  `-O2`.

#### Анализ экспериментов {#анализ-экспериментов .unnumbered}

Из результатов, представленных на скриншотах выше, видно явное влияние
уровня оптимизации на время работы функций:

- Для `-O0` (без оптимизаций) рекурсивная функция работала значительно
  медленнее (около 12--13 мс), тогда как итеративная занимала около
  $0.3$ мс. Это объясняется отсутствием каких-либо преобразований кода и
  значительными накладными расходами на рекурсивные вызовы.

- Для `-O1` и `-O2` время выполнения рекурсивной версии заметно
  сократилось до $2$--$4$ мс, а итеративная снизилась до
  $\approx 0.05$--$0.1$ мс.

- При `-O3` время работы итеративной функции упало до $\approx 0.03$ мс,
  рекурсивной --- до $1.4$ мс, что показывает, как агрессивные
  оптимизации могут уменьшить время работы, особенно для линейных
  алгоритмов.

- Для `-Os` время выполнения оказалось близким к `-O2`, но чуть быстрее
  для рекурсивного варианта, что свидетельствует о том, что оптимизации,
  направленные на уменьшение размера кода, в нашем случае не мешают
  скорости.

<!-- -->

- S. --- Static.

- D. --- Dynamic.

- I. --- Iterative.

- R. --- Recursive.

   Оптимизация   S. I. (мс)   S. R. (мс)   D. I. (мс)   D. R. (мс)
  ------------- ------------ ------------ ------------ ------------
       -O0         0.322        12.975       0.325        4.277
       -O1         0.088        4.047        0.055        2.576
       -O2         0.172        1.834        0.129        2.269
       -O3         0.052        1.490        0.030        1.481
       -Os         0.148        3.333        0.119        2.644

  : Абсолютное время работы функций, мс

$\Delta\%$ рассчитывается так:
$$\Delta\% = \frac{T_{\mathrm{-O0}} - T_{\mathrm{opt}}}{T_{\mathrm{-O0}}} \times 100\%$$

:

- $T_{-O0}$ --- время без оптимизации;

- $T_{opt}$ --- время с выбранной оптимизацией.

   Оптимизация   S. I. ($\Delta\%$)   S. R. ($\Delta\%$)   D. I. ($\Delta\%$)   D. R. ($\Delta\%$)
  ------------- -------------------- -------------------- -------------------- --------------------
       -O0               0%                   0%                   0%                   0%
       -O1             72.7%                68.8%                83.1%                39.8%
       -O2             46.6%                85.9%                60.3%                46.9%
       -O3             83.9%                88.5%                90.8%                65.4%
       -Os             54.0%                74.3%                63.4%                38.2%

  : Уменьшение времени относительно `-O0`, %

   Оптимизация      S. I.          S. R.           D. I.          D. R.
  ------------- -------------- -------------- --------------- --------------
       -O0       $1.00\times$   $1.00\times$   $1.00\times$    $1.00\times$
       -O1       $3.67\times$   $3.21\times$   $5.91\times$    $1.66\times$
       -O2       $1.87\times$   $7.09\times$   $2.52\times$    $1.88\times$
       -O3       $6.21\times$   $8.69\times$   $10.85\times$   $2.89\times$
       -Os       $2.17\times$   $3.90\times$   $2.73\times$    $1.62\times$

  : Прибавка к скорости выполнения по сравнению с `-O0`

#### Выводы по результатам экспериментов {#выводы-по-результатам-экспериментов .unnumbered}

Из полученных данных видно, что уровни оптимизации компилятора
существенно влияют на скорость выполнения программы. Для итеративной
реализации функции нахождения максимального элемента ускорение по
сравнению с отсутствием оптимизаций (`-O0`) достигает до $90\%$ при
использовании флага `-O3`. Рекурсивная реализация ускоряется менее
значительно --- примерно на $65\%$ для динамически слинкованного
варианта при `-O3`, что связано с тем, что компилятор может меньше
оптимизировать глубокие рекурсивные вызовы.

Кроме того, видно, что использование статической или динамической
линковки не оказывает критичного влияния на результаты, но в некоторых
случаях динамический вариант оказывается чуть быстрее, что можно
объяснить особенностями загрузки и размещения кода в памяти.

Оптимизация под размер кода (`-Os`) также показала хорошие результаты и
близка к `-O2` по скорости работы, особенно для рекурсивного варианта.

Таким образом, для поиска максимального в массиве наиболее эффективным
оказался уровень оптимизации `-O3`.

### Исследование разделяемых библиотек с помощью `ldd`

На скриншоте
[63](#fig:Screenshot_2025-06-30_at_16.40.24.png){reference-type="ref"
reference="fig:Screenshot_2025-06-30_at_16.40.24.png"} видно вывод
команды `ldd` для двух вариантов собранных программ:

![Вывод команды `ldd` для программ `test_dynamic` и
`test_static`.](img/Screenshot_2025-06-30_at_16.40.24.png){#fig:Screenshot_2025-06-30_at_16.40.24.png
width="90%"}

- `test_dynamic` --- программа, собранная с динамическим подключением
  библиотеки `libmax.so`;

- `test_static` --- программа, в которую код библиотеки `max` был
  встроен статически.

Из результатов видно следующее:

- Для `test_dynamic` явно указана зависимость от локальной библиотеки
  `libmax.so`, расположенной в каталоге сборки. Это значит, что при
  запуске программа загрузит код функции поиска максимального элемента
  именно из этой разделяемой библиотеки.

- Для `test_static` зависимости от `libmax.so` отсутствует, так как код
  был встроен в исполняемый файл на этапе компиляции. В выводе остаются
  лишь стандартные зависимости, такие как `libc.so.6`.

### Использование подгружаемых библиотек через `dlopen`

Для выполнения пункта задания, связанного с использованием механизма
подгружаемых библиотек, была модифицирована тестовая программа. В
существующем файле `tests/test.c` изменений не производилось, а вместо
этого был создан новый файл `tests/test_dlopen.c`, в котором функции
`max_iterative` и `max_recursive` загружаются динамически во время
исполнения программы с помощью `dlopen` и `dlsym`.

Основные отличия по сравнению с исходным вариантом заключаются в
следующем:

- Подключена библиотека `<dlfcn.h>` для использования функций `dlopen`,
  `dlsym` и `dlclose`.

- Добавлен вызов `dlopen("./libmax.so", RTLD_LAZY)` для загрузки
  динамической библиотеки во время выполнения.

- Получение адресов функций осуществляется через `dlsym`, после чего
  тестирование выполняется так же, как и в исходном варианте.

более подробное описание работы тестирующей программы:

- Получить адреса функций из библиотеки:

        max_iterative := dlsym(handle, "max_iterative")
        если ошибка через dlerror():
            вывести сообщение и завершить программу
        
        max_recursive := dlsym(handle, "max_recursive")
        если ошибка через dlerror():
            вывести сообщение и завершить программу

- Сгенерировать массив случайных чисел с помощью `myrand`.

- Замерить время работы полученных функций так же, как раньше:

        вызвать measure(max_iterative, arr, SIZE)
        вызвать measure(max_recursive, arr, SIZE)

- Закрыть библиотеку:

        dlclose(handle)

собирается командой:

    gcc tests/test_dlopen.c -ldl -o test_dlopen

Результаты тестирования при запуске программы приведены на скриншоте
ниже ---
[64](#fig:Screenshot_2025-07-01_at_11.38.34.png){reference-type="ref"
reference="fig:Screenshot_2025-07-01_at_11.38.34.png"}.

![Запуск тестовой программы с использованием `dlopen` и измерение
времени выполнения итеративной и рекурсивной функции поиска
максимального
элемента.](img/Screenshot_2025-07-01_at_11.38.34.png){#fig:Screenshot_2025-07-01_at_11.38.34.png
width="90%"}

Таким образом, главным отличием является то, что при `dlopen` загрузка и
связывание выполняется явно на уровне пользовательского кода во время
работы программы, что даёт возможность динамически выбирать загружаемую
библиотеку или обработать ошибку отсутствия библиотеки без аварийного
завершения программы.

## Сборка, тестирование и исследование зависимостей программы, осуществляющей работу с базой данных через псевдографический интерфейс

### Сборка программы

Для сборки программы был подготовлен файл `CMakeLists.txt`, который
задаёт проект на языке C, стандарт C99 и настраивает поиск необходимых
библиотек --- `ncursesw` (через `PkgConfig`) и `zlib`. После их
нахождения добавляется исполняемый файл `app`, которому передаются пути
к заголовочным файлам и библиотеки для линковки.

Редактирование данного файла производилось в редакторе `nano`, ниже
приведён псевдокод:

``` {.objectivec language="C" caption="Файл CMakeLists.txt"}
установить минимально необходимую версию cmake 3.10
задать имя проекта "znak_db" и язык программирования C

установить стандарт языка C на C99

поиск с помощью pkg-config библиотеки ncursesw (обязательно)
если не найдена, будет ошибка сборки

поиск установленной в системе библиотеки zlib (обязательно)

создать исполняемый файл "app" из файлов:
    - main.c
    - app.c

указать каталоги заголовочных файлов для target "app",
чтобы компилятор знал, где искать заголовки ncursesw

связать исполняемый файл "app" с:
    - найденной библиотекой ncursesw
    - найденной библиотекой zlib
```

После этого был создан каталог `build`, в который были сгенерированы
файлы сборочной системы командой `cmake ..`, и выполнена сама сборка
проекта с помощью утилиты `make`. На выходе получен исполняемый файл
`app`, который связывается с найденными библиотеками.

Ход выполнения этих команд и процесс сборки приведён на скриншоте
[65](#fig:Screenshot_2025-07-01_at_01.12.42.png){reference-type="ref"
reference="fig:Screenshot_2025-07-01_at_01.12.42.png"}.

![Конфигурация и сборка проекта с использованием CMake и
Make.](img/Screenshot_2025-07-01_at_01.12.42.png){#fig:Screenshot_2025-07-01_at_01.12.42.png
width="90%"}

Таким образом, проект был успешно собран с подключением библиотек
`ncursesw` для реализации псевдографического интерфейса и `zlib` для
вычисления контрольных сумм. Исполняемый файл готов к тестированию и
дальнейшему исследованию его зависимостей через команду `ldd`.

### Использование библиотеки `zlib` {#использование-библиотеки-zlib-1 .unnumbered}

Чтобы воспользоваться библиотекой `zlib`, необходимо выполнить несколько
шагов:

- **Установить саму библиотеку и её заголовочные файлы.** Для
  Debian/Ubuntu это делается командой:

      sudo apt install zlib1g-dev

  Пакет `zlib1g-dev` содержит как бинарную динамическую библиотеку
  (`libz.so`), так и файлы для компиляции (`zlib.h`).

- **Подключить заголовочный файл в исходном коде программы:**

      #include <zlib.h>

- **При сборке с помощью gcc добавить флаг линковки `-lz`**, чтобы
  компоновщик подключил библиотеку `zlib`:

      gcc main.c -lz -o app

  Если используется `CMake`, можно добавить:

      find_package(ZLIB REQUIRED)
      target_link_libraries(myapp ZLIB::ZLIB)

  Тогда `CMake` сам подставит правильные пути к библиотекам и
  заголовкам.

### Тестирование программы

Программа была протестирована вручную: с помощью псевдографического
интерфейса вводились новые записи, выполнялся просмотр заголовка и
содержимого базы данных, поиск по знаку зодиака и году рождения, а также
добавление новых записей. Результаты работы программы представлены на
скриншотах
[66](#fig:Screenshot_2025-07-01_at_01.43.35.png){reference-type="ref"
reference="fig:Screenshot_2025-07-01_at_01.43.35.png"}--[93](#fig:Screenshot_2025-07-01_at_01.58.10.png){reference-type="ref"
reference="fig:Screenshot_2025-07-01_at_01.58.10.png"}, которые
демонстрируют все основные этапы и корректное функционирование
программы.

![Запуск программы. Отсутствие базы данных и запрос на ввод количества
записей.](img/Screenshot_2025-07-01_at_01.43.35.png){#fig:Screenshot_2025-07-01_at_01.43.35.png
width="90%"}

![Ввод первой записи: фамилии, знака зодиака и года
рождения.](img/Screenshot_2025-07-01_at_01.44.06.png){#fig:Screenshot_2025-07-01_at_01.44.06.png
width="90%"}

![Главное меню программы с выбором
действий.](img/Screenshot_2025-07-01_at_01.44.12.png){#fig:Screenshot_2025-07-01_at_01.44.12.png
width="90%"}

![Просмотр заголовка базы данных с указанием количества записей и
контрольной
суммы.](img/Screenshot_2025-07-01_at_01.44.15.png){#fig:Screenshot_2025-07-01_at_01.44.15.png
width="90%"}

![Просмотр всех записей в базе данных в табличном
виде.](img/Screenshot_2025-07-01_at_01.44.20.png){#fig:Screenshot_2025-07-01_at_01.44.20.png
width="90%"}

![Просмотр обновлённого заголовка базы
данных.](img/Screenshot_2025-07-02_at_11.42.00.png){#fig:Screenshot_2025-07-02_at_11.42.00.png
width="90%"}

![Запрос параметров для поиска по знаку зодиака и году
рождения.](img/Screenshot_2025-07-01_at_01.44.33.png){#fig:Screenshot_2025-07-01_at_01.44.33.png
width="90%"}

![Результаты поиска записей по заданным
критериям.](img/Screenshot_2025-07-01_at_01.44.35.png){#fig:Screenshot_2025-07-01_at_01.44.35.png
width="90%"}

![Просмотр обновлённого заголовка базы
данных.](img/Screenshot_2025-07-02_at_11.42.38.png){#fig:Screenshot_2025-07-02_at_11.42.38.png
width="90%"}

![Добавление новой записи в базу
данных.](img/Screenshot_2025-07-01_at_01.46.06.png){#fig:Screenshot_2025-07-01_at_01.46.06.png
width="90%"}

![Просмотр обновлённой базы данных с двумя
записями.](img/Screenshot_2025-07-01_at_01.46.13.png){#fig:Screenshot_2025-07-01_at_01.46.13.png
width="90%"}

![Просмотр обновлённого заголовка базы
данных.](img/Screenshot_2025-07-02_at_11.46.12.png){#fig:Screenshot_2025-07-02_at_11.46.12.png
width="90%"}

![Удаление файла базы данных перед повторным
тестированием.](img/Screenshot_2025-07-01_at_01.47.33.png){#fig:Screenshot_2025-07-01_at_01.47.33.png
width="90%"}

![Запуск программы без базы данных и ввод некорректного количества
записей
(-1).](img/Screenshot_2025-07-01_at_01.47.45.png){#fig:Screenshot_2025-07-01_at_01.47.45.png
width="90%"}

![Сообщение о недопустимом количестве записей и предложение повторить
ввод.](img/Screenshot_2025-07-01_at_01.47.48.png){#fig:Screenshot_2025-07-01_at_01.47.48.png
width="90%"}

![Повторный запуск программы с вводом 20
записей.](img/Screenshot_2025-07-01_at_01.49.15.png){#fig:Screenshot_2025-07-01_at_01.49.15.png
width="90%"}

![Ввод записи с некорректным годом рождения вне допустимого
диапазона.](img/Screenshot_2025-07-01_at_01.50.06.png){#fig:Screenshot_2025-07-01_at_01.50.06.png
width="90%"}

![Сообщение о недопустимом году рождения и повторный
запрос.](img/Screenshot_2025-07-01_at_01.50.09.png){#fig:Screenshot_2025-07-01_at_01.50.09.png
width="90%"}

![Повторный воод года
рождения.](img/Screenshot_2025-07-01_at_01.50.58.png){#fig:Screenshot_2025-07-01_at_01.50.58.png
width="90%"}

![Сообщение о пустом знаке зодиака и повторный
запрос.](img/Screenshot_2025-07-01_at_01.51.11.png){#fig:Screenshot_2025-07-01_at_01.51.11.png
width="90%"}

![Просмотр заголовка базы данных после ввода
записей.](img/Screenshot_2025-07-01_at_01.55.18.png){#fig:Screenshot_2025-07-01_at_01.55.18.png
width="90%"}

![Просмотр всех записей базы данных в табличном виде после ввода 20
элементов.](img/Screenshot_2025-07-01_at_01.55.29.png){#fig:Screenshot_2025-07-01_at_01.55.29.png
width="90%"}

![Просмотр вротой страницы всех записей базы данных в табличном виде
после ввода 20
элементов.](img/Screenshot_2025-07-01_at_01.55.32.png){#fig:Screenshot_2025-07-01_at_01.55.32.png
width="90%"}

![Поиск среди множества записей по знаку зодиака `f` и году
`2000`](img/Screenshot_2025-07-01_at_01.56.16.png){#fig:Screenshot_2025-07-01_at_01.56.16.png
width="90%"}

![Поиск заведомо некорректными
данными](img/Screenshot_2025-07-01_at_01.56.59.png){#fig:Screenshot_2025-07-01_at_01.56.59.png
width="90%"}

![Добавление записи на имя
`petrov v v`](img/Screenshot_2025-07-01_at_01.57.51.png){#fig:Screenshot_2025-07-01_at_01.57.51.png
width="90%"}

![Просмотр содержимого второй страницы обновлённой базы
данных.](img/Screenshot_2025-07-01_at_01.58.00.png){#fig:Screenshot_2025-07-01_at_01.58.00.png
width="90%"}

![Просмотр заголовка новой базы данных после создания --- указано
количество записей и контрольная
сумма.](img/Screenshot_2025-07-01_at_01.58.10.png){#fig:Screenshot_2025-07-01_at_01.58.10.png
width="90%"}

### Просмотр бинарного файла базы данных в шестнадцатеричном виде

Для проверки корректности структуры и содержимого бинарного файла базы
данных была использована утилита `hexdump` с параметром `-C`,
позволяющим вывести данные в шестнадцатеричном виде с расшифровкой в
ASCII. Ниже на скриншоте
[94](#fig:Screenshot_2025-07-01_at_03.18.28.png){reference-type="ref"
reference="fig:Screenshot_2025-07-01_at_03.18.28.png"} видно, что файл
начинается с сигнатуры `SEME` (в шестнадцатеричном виде `53 45 4D 45`),
далее идут служебные поля заголовка (номер транзакции, количество
записей, контрольная сумма CRC32), а затем записи о пользователях. Таким
образом, структура файла полностью соответствует ожидаемому формату.

![Шестнадцатеричный дамп бинарного файла базы данных, полученный с
помощью
`hexdump -C znak_db`.](img/Screenshot_2025-07-01_at_03.18.28.png){#fig:Screenshot_2025-07-01_at_03.18.28.png
width="90%"}

### Исследование разделяемых библиотек и построение дерева зависимостей

После выполнения задания и получения исполняемого файла программы были
исследованы её зависимости от разделяемых библиотек с помощью команды
`ldd`. На скриншоте
[95](#fig:Screenshot_2025-07-01_at_03.38.15.png){reference-type="ref"
reference="fig:Screenshot_2025-07-01_at_03.38.15.png"} приведён
результат анализа исполняемого файла `app` и его библиотек.

![Вывод команды `ldd` для программы и зависимых
библиотек.](img/Screenshot_2025-07-01_at_03.38.15.png){#fig:Screenshot_2025-07-01_at_03.38.15.png
width="90%"}

На основании результатов команды `ldd` было построено рекурсивное дерево
зависимостей программы (рис.
[96](#fig:app-ldd-tree){reference-type="ref"
reference="fig:app-ldd-tree"}).

<figure id="fig:app-ldd-tree">

<figcaption>Рекурсивное дерево зависимостей программы <code>app</code>,
построенное с использованием <code>ldd</code></figcaption>
</figure>

# Заключение {#заключение .unnumbered}

В ходе выполнения работы были выполнены все задачи, поставленные в
разделе постановки задачи, с использованием современных средств
разработки на платформе GNU/Linux в виртуальной среде VirtualBox.
Подробно опишем каждый пункт:

- Разработана библиотека на языке Си для нахождения максимального
  элемента в массиве, содержащая две реализации: рекурсивную и
  итеративную. В реализации не использовались библиотечные функции.

- Полученные исходные файлы библиотеки были собраны в два варианта:

  - статическая библиотека (`.a`), компонуемая на этапе сборки вместе с
    программой,

  - динамическая библиотека (`.so`), подключаемая при запуске программы.

  Для сборки использовалась система `cmake`, что упростило процесс
  компиляции и управления флагами компилятора.

- Создана тестовая программа на Си, измеряющая время выполнения функций
  поиска максимального элемента при различных вариантах использования
  библиотеки (статическая, динамическая линковка). Для измерений времени
  использовались функции `gettimeofday` и вывод результатов в
  миллисекундах.

- Программа была дополнена вариантами компиляции с разными уровнями
  оптимизации компилятора (`-O0`, `-O1`, `-O2`, `-O3`, `-Os`). Для
  автоматизации сборки с различными флагами использован скрипт на
  `bash`. Проведены сравнительные эксперименты, показавшие значительное
  ускорение выполнения кода при включении оптимизаций, особенно для
  итеративного варианта.

- Для анализа работы динамической загрузки функций написан отдельный
  тест, в котором функции `max_iterative` и `max_recursive` подгружались
  во время выполнения через `dlopen` и `dlsym`. Это позволило оценить
  плюсы (гибкость загрузки) и минусы (потенциальные накладные расходы и
  необходимость явного контроля ошибок загрузки) подхода по сравнению с
  обычной динамической линковкой.

- С помощью утилиты `ldd` были исследованы зависимости скомпилированных
  исполняемых файлов от системных разделяемых библиотек. Результаты
  использовались для построения рекурсивного дерева зависимостей в
  отчёте, что позволило визуально увидеть, какие библиотеки требуются
  для работы программы.

- Реализована структура данных `ZNAK`, включающая фамилию и инициалы
  (строка), знак зодиака (строка) и год рождения (целое число). Также
  определён заголовок бинарного файла базы данных, содержащий сигнатуру
  (4 байта), номер транзакции, количество записей и CRC-32 контрольную
  сумму.

- Разработана полноценная программа управления бинарной базой данных с
  использованием библиотеки `ncurses` для создания псевдографического
  интерфейса. Программа поддерживает:

  - создание базы данных при её отсутствии с вводом массива записей
    структуры `ZNAK`,

  - поиск людей по году рождения и знаку зодиака с выводом результатов,

  - добавление новых записей в базу данных с автоматическим обновлением
    номера транзакции и CRC-32.

  Благодаря использованию `ncurses`, интерфейс программы адаптируется к
  размеру терминала и обеспечивает постраничный вывод данных.

- С помощью утилиты `hexdump` были зафиксированы промежуточные состояния
  бинарного файла базы после различных операций (создание, добавление
  записей), что подтвердило изменение контрольной суммы и номера
  транзакции в заголовке при каждом обновлении базы.

- Для всех программ и библиотек использовались стандартные средства
  GNU/Linux: компилятор `gcc`, система сборки `cmake`, пакет
  `build-essential`, а также установлены и использованы библиотеки
  `ncurses` и `zlib`, необходимые для работы с текстовым интерфейсом и
  расчёта CRC-32 соответственно.

В итоге были освоены практические приёмы работы со статическими и
динамическими библиотеками, управление зависимостями, создание
интерфейсов в текстовом режиме с помощью `ncurses`, работа с бинарными
файлами и их контроль целостности с использованием `zlib`. Кроме того,
получены навыки работы в терминальной среде Linux и использования
инструментов для анализа зависимостей и отладки бинарных файлов.

::: thebibliography
99

GCC --- GNU Compiler Collection \[Электронный ресурс\] // man7.org Linux
man-pages. URL: <https://man7.org/linux/man-pages/man1/gcc.1.html> (дата
обращения: 01.07.2025).

CMake Documentation \[Электронный ресурс\] // Kitware. URL:
<https://cmake.org/cmake/help/latest/> (дата обращения: 26.06.2025).

ldd(1) --- print shared object dependencies \[Электронный ресурс\] //
man7.org Linux man-pages. URL:
<https://man7.org/linux/man-pages/man1/ldd.1.html> (дата обращения:
26.07.2025).

dlopen(3) --- dynamic loading of shared libraries \[Электронный ресурс\]
// man7.org Linux man-pages. URL:
<https://man7.org/linux/man-pages/man3/dlopen.3.html> (дата обращения:
30.06.2025).

Ncurses Programming HOWTO \[Электронный ресурс\] // Linux Documentation
Project. URL: <https://tldp.org/HOWTO/NCURSES-Programming-HOWTO/> (дата
обращения: 28.06.2025).

zlib --- A Massively Spiffy Yet Delicately Unobtrusive Compression
Library \[Электронный ресурс\]. URL: <https://zlib.net/manual.html>
(дата обращения: 28.06.2025).

hexdump(1) --- display file contents in hexadecimal, decimal, octal, or
ASCII \[Электронный ресурс\] // man7.org Linux man-pages. URL:
<https://man7.org/linux/man-pages/man1/hexdump.1.html> (дата обращения:
29.07.2025).

ssh(1) --- OpenSSH remote login client \[Электронный ресурс\] //
man7.org Linux man-pages. URL:
<https://man7.org/linux/man-pages/man1/ssh.1.html> (дата обращения:
18.06.2025).

Oracle VM VirtualBox User Manual \[Электронный ресурс\] // Oracle. URL:
<https://www.virtualbox.org/manual/UserManual.html> (дата обращения:
18.06.2025).

man \... -- даёт стравку о функции в терминале GNU/Linux. (дата
обращения: 20.06.2025).
:::

# Приложение A. Задание 1. Заголовочный файл библиотеки поиска максимально элемента {#приложение-a.-задание-1.-заголовочный-файл-библиотеки-поиска-максимально-элемента .unnumbered}

``` {.numberLines .objectivec language="C" numbers="left"}
int max_iterative(const int *arr, int size);
int max_recursive(const int *arr, int size);

double max_iterative_double(const double *arr, int size);
double max_recursive_double(const double *arr, int size);

char max_iterative_char(const char *arr, int size);
char max_recursive_char(const char *arr, int size);
```

# Приложение Б. Задание 1. Реализация функций библиотеки поиска максимально элемента {#приложение-б.-задание-1.-реализация-функций-библиотеки-поиска-максимально-элемента .unnumbered}

``` {.numberLines .objectivec language="C" numbers="left"}
#include "max.h"

int max_iterative(const int *arr, int size) {
  if (size <= 0) return 0;
  int max = arr[0];
  for (int i = 1; i < size; ++i) {
    if (arr[i] > max)
      max = arr[i];
  }
  return max;
}

int max_recursive(const int *arr, int size) {
  if (size <= 0) return 0;
  if (size == 1)
    return arr[0];
  int m = max_recursive(arr, size - 1);
  return (arr[size - 1] > m) ? arr[size - 1] : m;
}

double max_iterative_double(const double *arr, int size) {
  if (size <= 0) return 0.0;
  double max = arr[0];
  for (int i = 1; i < size; ++i) {
    if (arr[i] > max)
      max = arr[i];
  }
  return max;
}

double max_recursive_double(const double *arr, int size) {
  if (size <= 0) return 0.0;
  if (size == 1)
    return arr[0];
  double m = max_recursive_double(arr, size - 1);
  return (arr[size - 1] > m) ? arr[size - 1] : m;
}

char max_iterative_char(const char *arr, int size) {
  if (size <= 0) return '\0';
  char max = arr[0];
  for (int i = 1; i < size; ++i) {
    if (arr[i] > max)
      max = arr[i];
  }
  return max;
}

char max_recursive_char(const char *arr, int size) {
  if (size <= 0) return '\0';
  if (size == 1)
    return arr[0];
  char m = max_recursive_char(arr, size - 1);
  return (arr[size - 1] > m) ? arr[size - 1] : m;
}
```

# Приложение В. Задание 1. Реализация тестирующей функций библиотеки поиска максимально элемента {#приложение-в.-задание-1.-реализация-тестирующей-функций-библиотеки-поиска-максимально-элемента .unnumbered}

``` {.numberLines .objectivec language="C" numbers="left"}
// test.c

#include <stdio.h>
#include <sys/time.h>
#include "max.h"

unsigned int seed = 123456789;
int myrand() {
  seed = seed * 1103515245 + 12345;
  return (seed >> 16) & 0x7FFF;
}

double measure(int (*func)(const int*, int), const int* arr, int size) {
  struct timeval start, end;
  gettimeofday(&start, NULL);
  int result = func(arr, size);
  gettimeofday(&end, NULL);

  double elapsed = (end.tv_sec - start.tv_sec) * 1000.0;
  elapsed += (end.tv_usec - start.tv_usec) / 1000.0;

  printf("Result: %d, Time: %.3f ms\n", result, elapsed);
  return elapsed;
}

int main() {
  const int SIZE = 100000;
  int arr[SIZE];

  for (int i = 0; i < SIZE; ++i)
      arr[i] = myrand();

  printf("Iterative max:\n");
  measure(max_iterative, arr, SIZE);

  printf("Recursive max:\n");
  measure(max_recursive, arr, SIZE);

  return 0;
}

// test_dlopen.c

#include <stdio.h>
#include <stdlib.h>
#include <dlfcn.h>
#include <sys/time.h>

unsigned int seed = 123456789;
int myrand() {
  seed = seed * 1103515245 + 12345;
  return (seed >> 16) & 0x7FFF;
}

double measure(int (*func)(const int*, int), const int* arr, int size) {
  struct timeval start, end;
  gettimeofday(&start, NULL);
  int result = func(arr, size);
  gettimeofday(&end, NULL);

  double elapsed = (end.tv_sec - start.tv_sec) * 1000.0;
  elapsed += (end.tv_usec - start.tv_usec) / 1000.0;

  printf("Result: %d, Time: %.3f ms\n", result, elapsed);
  return elapsed;
}

int main() {
  void *handle;
  int (*max_iterative)(const int*, int);
  int (*max_recursive)(const int*, int);
  char *error;

  // Открываем библиотеку
  handle = dlopen("./libmax.so", RTLD_LAZY);
  if (!handle) {
    fprintf(stderr, "Ошибка загрузки: %s\n", dlerror());
    exit(EXIT_FAILURE);
  }

  // Получаем адреса функций
  max_iterative = (int (*)(const int*, int)) dlsym(handle, "max_iterative");
  if ((error = dlerror()) != NULL)  {
    fprintf(stderr, "Ошибка поиска max_iterative: %s\n", error);
    exit(EXIT_FAILURE);
  }

  max_recursive = (int (*)(const int*, int)) dlsym(handle, "max_recursive");
  if ((error = dlerror()) != NULL)  {
    fprintf(stderr, "Ошибка поиска max_recursive: %s\n", error);
    exit(EXIT_FAILURE);
  }

  // Заполняем массив и тестируем
  const int SIZE = 100000;
  int arr[SIZE];
  for (int i = 0; i < SIZE; ++i)
      arr[i] = myrand();

  printf("Iterative max via dlopen:\n");
  measure(max_iterative, arr, SIZE);

  printf("Recursive max via dlopen:\n");
  measure(max_recursive, arr, SIZE);

  dlclose(handle);
  return 0;
}
```

# Приложение Г. Задание 2. Заголовочный файл, описывающий заголовок базы данных и структуру `ZNAK` {#приложение-г.-задание-2.-заголовочный-файл-описывающий-заголовок-базы-данных-и-структуру-znak .unnumbered}

``` {#lst:move_array .numberLines .objectivec language="C" label="lst:move_array" numbers="left"}
#define NAME_LEN 64
#define ZODIAC_LEN 32

#include <stdint.h>
#include <zlib.h>

typedef struct {
    char name[NAME_LEN];
    char zodiac[ZODIAC_LEN];
    int birth_year;
} ZNAK;

typedef struct {
    char signature[4];
    uint32_t transaction_id;
    uint32_t count;
    uint32_t checksum;
} ZNAK_DB_HEADER;
```

# Приложение Д. Задание 2. Заголовочный файл, описывающий функции работы с псевдографическим интерфейсом и базой данных {#приложение-д.-задание-2.-заголовочный-файл-описывающий-функции-работы-с-псевдографическим-интерфейсом-и-базой-данных .unnumbered}

``` {.numberLines .objectivec language="C" numbers="left"}
#include "znak.h"

#define DB_FILENAME "znak_db"
#define MAX_RECORDS 100

void input_znak(ZNAK *z);
void save_db(ZNAK_DB_HEADER *header, ZNAK *arr, int count);
void find_by_year_and_zodiac(ZNAK_DB_HEADER *header, 
ZNAK *arr, int count, const char *zodiac, int year);
void add_record(ZNAK_DB_HEADER *header, ZNAK *arr, int *count);
void search_records(ZNAK_DB_HEADER *header, ZNAK *arr, int count);
void print_db_header(ZNAK_DB_HEADER *header);
void db_print_interface(ZNAK_DB_HEADER *header, ZNAK *arr, int count);
void print_db(ZNAK_DB_HEADER *header, ZNAK *arr, int count);
void error_msg(const char *msg);
void clear_except_first_line();
```

# Приложение Е. Задание 2. Реализация функций работы с псевдографическим интерфейсом и базой данных {#приложение-е.-задание-2.-реализация-функций-работы-с-псевдографическим-интерфейсом-и-базой-данных .unnumbered}

``` {.numberLines .objectivec language="C" numbers="left"}
#include <string.h>
#include <stdio.h>
#include <stdlib.h>
#include <ncurses.h>
#include <zlib.h>
#include "app.h"

void input_znak(ZNAK *z) {
  echo();
  while (1) {
    mvprintw(2, 2, "Фамилия и инициалы: ");
    getnstr(z->name, sizeof(z->name) - 1);
    if (strlen(z->name) > 0) break;
    error_msg("Фамилия и инициалы не могут быть пустыми.");
  }

  while (1) {
    mvprintw(3, 2, "Знак зодиака: ");
    getnstr(z->zodiac, sizeof(z->zodiac) - 1);
    if (strlen(z->zodiac) > 0) break;
    error_msg("Знак зодиака не может быть пустым.");
  }

  while (1) {
    int birth_year = 0;
    mvprintw(4, 2, "Год рождения: ");
    scanw("%d", &birth_year);
    if (birth_year >= 1899 && birth_year <= 2025) {
      z->birth_year = birth_year;
      break;
    } else {
      error_msg("Год рождения должен быть в диапазоне 1899-2025.");
    }
  }
  noecho();
}

void save_db(ZNAK_DB_HEADER *header, ZNAK *arr, int count) {
  FILE *file = fopen(DB_FILENAME, "wb");
  if (!file) {
    perror("Ошибка создания файла");
    return;
  }

  header->count = count;
  header->transaction_id++;
  header->checksum = crc32(0L, Z_NULL, 0);
  header->checksum = crc32(header->checksum, (const Bytef*)arr, sizeof(ZNAK) * count);

  fwrite(header, sizeof(ZNAK_DB_HEADER), 1, file);
  fwrite(arr, sizeof(ZNAK), count, file);
  fclose(file);
}

void find_by_year_and_zodiac(ZNAK_DB_HEADER *header, 
  ZNAK *arr, int count, const char *zodiac, int year) {
  save_db(header, arr, count);

  int found = 0;

  ZNAK found_arr[count];
  for (int i = 0; i < count; ++i) {
    if (arr[i].birth_year == year && \
      strcmp(arr[i].zodiac, zodiac) == 0) {
      found_arr[found++] = arr[i];
    }
  }
  
  clear();
  mvprintw(1, 2, "Результаты поиска по знаку \"%s\" и году %d:",
          zodiac, year);

  if (found) {
    db_print_interface(header, found_arr, found);
  } else {
    mvprintw(5, 4, "Совпадений не найдено.");
    mvprintw(20, 2, "Нажмите любую клавишу для выхода...");
    getch();
  }
}

void add_record(ZNAK_DB_HEADER *header, ZNAK *arr, int *count) {
  clear();
  mvprintw(1, 2, "Добавление новой записи:");
  input_znak(&arr[*count]);
  (*count)++;
  save_db(header, arr, *count);
}

void search_records(ZNAK_DB_HEADER *header, ZNAK *arr, int count) {
  char search_zodiac[ZODIAC_LEN];
  int search_year;

  clear();
  mvprintw(1, 2, "Введите знак зодиака: ");
  echo();
  getnstr(search_zodiac, sizeof(search_zodiac) - 1);
  noecho();

  mvprintw(2, 2, "Введите год рождения: ");
  echo();
  scanw("%d", &search_year);
  noecho();

  find_by_year_and_zodiac(header, arr, count, search_zodiac,
  search_year);
}

void print_db_header(ZNAK_DB_HEADER *header) {
  clear();
  mvprintw(1, 2, "Заголовок базы данных:");
  mvprintw(2, 4, "Сигнатура: %.4s", header->signature);
  mvprintw(3, 4, "Номер транзакции: %u", header->transaction_id);
  mvprintw(4, 4, "Количество записей: %u", header->count);
  mvprintw(5, 4, "Контрольная сумма (CRC32): %u", header->checksum);

  mvprintw(20, 2, "Нажмите любую клавишу для выхода...");
  getch();
}

void print_db(ZNAK_DB_HEADER *header, ZNAK *arr, int count) {
  clear();
  save_db(header, arr, count);
  mvprintw(1, 2, "Содержимое базы данных:");
  db_print_interface(header, arr, count);
}

void clear_except_first_line() {
  int rows, cols;
  getmaxyx(stdscr, rows, cols);

  for (int i = 2; i <= rows; ++i) {
    move(i, 0);
    clrtoeol();
  }
}

void db_print_interface(ZNAK_DB_HEADER *header, ZNAK *arr, int count) {
  int rows, cols;
  getmaxyx(stdscr, rows, cols);
  
  int lines_per_page = rows - 10;
  int start = 0;
  
  while (start < count) {
    clear_except_first_line();
    mvprintw(2, 4, "%-7s %-41s %-26s %-5s", "№", 
    "Фамилия и инициалы", "Знак зодиака", "Год");
    mvhline(3, 4, '-', cols - 8);

    int i, row = 4;
    for (i = start; i < count && i < start + lines_per_page; ++i) {
      mvprintw(row++, 4, "%-5d %-25s %-15s %-5d",
                i + 1, arr[i].name, arr[i].zodiac, arr[i].birth_year);
    }

    mvprintw(rows - 2, 2,
"Страница %d/%d. Нажмите 'n' для следующей, 'p' для 
предыдущей, 'q' для выхода.",
      (start / lines_per_page) + 1,
      (count + lines_per_page - 1) / lines_per_page);

    int ch = getch();
    if (ch == 'q' || ch == 'Q')
      break;
    else if (ch == 'n' || ch == 'N')
      start += lines_per_page;
    else if (ch == 'p' || ch == 'P') {
      if (start - lines_per_page >= 0)
        start -= lines_per_page;
    } else
      break;
  }
}

void error_msg(const char *msg) {
  clear();
  mvprintw(1, 2, msg);
  mvprintw(3, 2, "Нажмите любую клавишу для повторной попытки...");
  getch();
  clear();
}
```

# Приложение Ё. Задание 2. Реализация главной управляющей функции программы {#приложение-ё.-задание-2.-реализация-главной-управляющей-функции-программы .unnumbered}

``` {.numberLines .objectivec language="C" numbers="left"}
#include <string.h>
#include <ncurses.h>
#include <locale.h>
#include "app.h"

int main() {
  setlocale(LC_ALL, "ru_RU.UTF-8");
  initscr();
  cbreak();
  noecho();

  ZNAK records[MAX_RECORDS];
  int count = 0;

  ZNAK_DB_HEADER header;

  FILE *file = fopen(DB_FILENAME, "rb");
  if (!file) {
    memcpy(header.signature, "SEME", 4);
    header.transaction_id = 0;
    header.count = 0;
    header.checksum = 0;

    while (1) {
      clear();
      mvprintw(1, 2, "Файл не найден. Введите количество записей: ");
      echo();
      scanw("%d", &count);
      noecho();

      if (count <= MAX_RECORDS && count >= 1) {
        for (int i = 0; i < count; ++i) {
          clear();
          mvprintw(1, 2, "Запись %d", i + 1);
          input_znak(&records[i]);
        }

        save_db(&header, records, count);
        break;
      } else {
        error_msg("Количество должно быть числом от 1 до 100.");
      }
    }
  } else {
    fread(&header, sizeof(ZNAK_DB_HEADER), 1, file);
    count = header.count;
    fread(records, sizeof(ZNAK), count, file);
    fclose(file);
  }

  int choice;
  while (1) {
    clear();
    mvprintw(2, 2, "1. Просмотр заголовка базы данных");
    mvprintw(3, 2, "2. Просмотр всех записей базы данных");
    mvprintw(4, 2, "3. Поиск по знаку зодиака и году");
    mvprintw(5, 2, "4. Добавить запись");
    mvprintw(6, 2, "5. Выход");
    mvprintw(8, 2, "Ваш выбор: ");
    echo();
    scanw("%d", &choice);
    noecho();

    if (choice == 1) {
      print_db_header(&header);
    } else if (choice == 2) {
      print_db(&header, records, count);
    } else if (choice == 3) {
      search_records(&header, records, count);
    } else if (choice == 4) {
      add_record(&header, records, &count);
    } else if (choice == 5) {
      break;
    }
  }
  endwin();
  return 0;
}
```
