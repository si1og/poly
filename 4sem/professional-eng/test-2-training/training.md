#2.1.

- `computer accepts data in a certain form, process it, gives results in a special format as information `
- `data is fed into the computer's memory`
- when `program is run`, the computer `performs a set of instructions` and `processes the data`
- `hardware` is any electronic or mechanical part
- `software` (set of instructions / programs) is installed on a computer
- basic hardware sections: `central processing unit` (CPU), `main memory` and `peripherals`
- `execute program instructions`
- `coordinate activities` of other units
- `hold instructions and data` processed by CPU
- `storage devices, input/output devices`
- `disk drivers` are used to read and write data on disks
- `enable data to go into the computer's memory`
- `on the rear / front panel`
- `USB port`

#2.2.
- `type / enter a command`
- tell a computer what to do `(input)`
- system software tells the CPU to `start up certain programs`
- `turn on hardware devices`
- `enter / click on an address (URL - uniform resource loader)`
- `browser software goes out to find the address`
- `start up hardware devices`

#3.1
- `take up entire rooms`
- `fit on desktop / in a phone / on a chip the size of the rice grain`
- `evolve from a cumbersome and slow device to quick and smart machines`
- machines with `incredible processing power`
- `be credited with inventing / creating / building / developing a computer`
- difficult aspect of `completing large calculations`
- `slide rule`
- `mechanical adding machine`
- `ability to keep track of many results`
- `be created with a focus on basic elements`
- `have a control, have a memory to store results`, to `perform calculations`

#3.2
- scienetists and ma
